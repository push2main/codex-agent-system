.board-tools {
  display: grid;
  gap: 10px;
}

.board-search {
  display: block;
}

.board-search input {
  min-height: 46px;
  padding-left: 16px;
}

.density-toggle {
  display: inline-grid;
  grid-auto-flow: column;
  grid-auto-columns: minmax(0, 1fr);
  gap: 8px;
}

.density-chip {
  min-width: 0;
  padding: 10px 12px;
  border-radius: 999px;
  border: 1px solid rgba(133, 161, 188, 0.14);
  color: rgba(230, 239, 246, 0.72);
  background: rgba(255, 255, 255, 0.03);
  font-weight: 700;
  letter-spacing: 0.02em;
}

.density-chip.active {
  color: #08111a;
  background: linear-gradient(135deg, #dffaf6, #6ce7db);
  border-color: transparent;
}

.task-search-note {
  margin: 0;
  min-height: 1.2rem;
  color: var(--muted);
  font-size: 0.84rem;
  line-height: 1.4;
}

.task-search-note strong {
  color: #f4fbff;
  font-weight: 700;
}

.search-empty-state {
  padding: 12px 0 2px;
  color: var(--muted);
  font-size: 0.84rem;
}

.task-item[data-search-hidden="true"] {
  display: none;
}

.task-column[data-search-empty="true"] {
  border-color: rgba(133, 161, 188, 0.2);
}

.task-column[data-search-empty="true"] .task-column-header {
  opacity: 0.78;
}

.item.task-item mark,
.queue-item mark {
  padding: 0 0.2em;
  border-radius: 0.35em;
  color: inherit;
  background: rgba(108, 231, 219, 0.18);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

body[data-density="compact"] .task-board-shell {
  gap: 10px;
  padding: 10px;
}

body[data-density="compact"] .task-board {
  gap: 10px;
}

body[data-density="compact"] .task-column,
body[data-density="compact"] .task-board-toolbar,
body[data-density="compact"] .live-work-strip,
body[data-density="compact"] .task-item,
body[data-density="compact"] .queue-item,
body[data-density="compact"] .detail-panel {
  padding: 12px;
}

body[data-density="compact"] .task-column {
  gap: 10px;
}

body[data-density="compact"] .item.task-item,
body[data-density="compact"] .queue-item {
  gap: 10px;
}

body[data-density="compact"] .item-title {
  font-size: 0.95rem;
}

body[data-density="compact"] .item-copy,
body[data-density="compact"] .muted,
body[data-density="compact"] .task-search-note {
  font-size: 0.82rem;
}

body[data-density="compact"] .meta {
  gap: 6px;
}

body[data-density="compact"] .tag {
  padding: 5px 8px;
  font-size: 0.72rem;
}

body[data-density="compact"] .task-details {
  padding-top: 8px;
  margin-top: 8px;
}

@media (min-width: 900px) {
  .board-tools {
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
  }
}

@media (max-width: 767px) {
  .density-toggle {
    width: 100%;
  }

  .density-chip {
    min-height: 42px;
  }
}
