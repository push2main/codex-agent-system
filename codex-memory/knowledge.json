{
  "rules": [
    {
      "category": "stability",
      "when": "approved tasks are stored outside the runtime queue",
      "rule": "surface them in the dashboard until a deterministic approval-to-queue bridge exists"
    },
    {
      "category": "ui",
      "when": "dashboard controls are simplified",
      "rule": "keep mobile-first cards for status, queue, logs, and pending approvals"
    },
    {
      "category": "ui",
      "when": "task registry items span multiple states",
      "rule": "group them by status and show a single recommended next action"
    },
    {
      "category": "code_quality",
      "when": "task registry items are used across multiple runs",
      "rule": "store stable ids and timestamps so future state transitions and audits are deterministic"
    },
    {
      "category": "stability",
      "when": "approval actions hand tasks into execution",
      "rule": "approve through the same queue safety checks used by manual submissions and append an audit history entry"
    },
    {
      "category": "stability",
      "when": "queue execution changes the real state of an approved registry task",
      "rule": "mirror the runtime outcome back into tasks.json with deterministic history entries so approved work does not stay stale after execution"
    },
    {
      "category": "ui",
      "when": "task board cards need retry and completion context",
      "rule": "normalize execution and history fields in the task-registry API before rendering them in the mobile dashboard"
    },
    {
      "category": "stability",
      "when": "approved work is recovered directly from a verified working tree",
      "rule": "clear stale queue entries and mark the registry task complete in the same change so the loop does not re-run recovered work"
    },
    {
      "category": "stability",
      "when": "manual recovery completes approved work outside the queue",
      "rule": "append a deterministic manual_recovery record to tasks.log and regenerate metrics.json from the registry snapshot so observability reflects the recovered outcome"
    }
  ]
}
