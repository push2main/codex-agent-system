- 2026-03-29T20:07:26Z | SUCCESS | Turn the cloud-brain README into a concrete runtime blueprint
  Rules: In `agents/planner.sh`, add a preflight `detect_noop_anchor_task()` that runs before step generation for comment-only and line-specific edit tasks; if the task text contains `already exists`, `confirm`, `inspect`, or `line ` and the requested target text is already present in the named file, mark the task `non_retriable_review_rejection` and skip coder/reviewer execution. This directly targets the repeated no-op comment tasks that were retried and rejected at least 4 times.; In `agents/planner.sh`, reject any step-1 instruction longer than `420` characters or containing more than `2` quoted expectations (`Expected:` clauses, parenthetical enumerations, or `line N` anchors); auto-split into `inspect anchor` and `edit` steps, with verification as the final step. This is enforceable in the existing step-scope gate and should reduce review rejections caused by overloaded first-step instructions, which appear across the repeated test/comment failures.; In `agents/orchestrator.sh`, add a retry guard keyed by `task_title + failed_step text hash`; if a task fails twice with `review_rejection` and the normalized step-1 text similarity is `>= 0.85`, shelve it instead of requeueing. This prevents the observed repeated retries on the same rejected plan shape and should eliminate at least the 2-task churn clusters shown for `classify_retry_failure`, `clamp_prompt_context`, and planner-step-cap tasks.
- 2026-03-29T20:11:43Z | SUCCESS | In agents/learner.sh, update the dedup comment to document the 65% threshold change
  Rules: In `agents/planner.sh`, reject any step text longer than `450` characters before dispatch, not just `600`; if `len(step) > 450`, split it at sentence boundaries into multiple steps and keep the last step as verification. This targets the repeated `review_rejection` tasks whose step payloads are 500 to 1200+ chars and should cut reviewer fallback/rejection on dense single-step tasks.; In `agents/planner.sh` step assembly, if a step contains both an inspection clause and an edit clause, detected by mixed verbs such as `inspect|read|confirm` plus `change|replace|create|update`, split it into two steps automatically. At least 6 recent failures combined “inspect first, then edit/create” in one step; enforcing one intent per step should prevent reviewer rejection for ambiguous execution.; In `agents/planner.sh`, add a no-op task precheck for comment/documentation edits: if the task title matches `comment|document|update.*comment` and the target file already contains the requested literal token or percentage, emit a single verification-only plan and classify completion as `no_op_already_present` without an edit step. This directly addresses the repeated learner/planner comment failures that were retried despite the requested text already existing.
- 2026-03-30T00:02:32Z | SUCCESS | [self-improve:critical] Remove automation runtime example from incident schema -- Start with packages/schema/incident.sc
  Rules: In the task generator function that builds step text for self-improve tasks, if the declared file path ends in `.json`, `.yaml`, or `.yml`, hard-ban any step text containing code-file suffixes or code anchors such as `\.js`, `\.ts`, `function`, `branch`, or `state transition`; regenerate the step before dispatch. This directly targets the repeated `incident.schema.json` -> `incident.schema.js` mismatch and should eliminate the 3 recent structured-data missing-file failures.; In orchestrator preflight, before step 1 starts, extract every backticked or path-like token from the full task and all planned steps; if any referenced path is not in the declared `files:` list and does not exist under the repo root, fail classification as `missing_source_file` immediately and block execution. Enforce this in the step-validation path, not reviewer output, to prevent 1-2 wasted step attempts per bad plan.; In the planner for self-improve tasks, require a literal file-existence probe for each non-verify step target before emitting the plan; if the probe count is `0` or `>1`, reject the plan and re-plan with exactly one existing file. Apply this in the planner’s final validation hook to reduce both `missing_source_file` and step-2 timeouts caused by searching for anchors in the wrong file.
- 2026-03-30T11:08:40Z | SUCCESS | [self-improve:medium] Document mandatory MVP protection cases in first slice -- Start with docs/architecture/first-slice
  Rules: In `orchestrator` task pre-check, if the task text contains any explicit path in backticks, `(files: ...)`, or `Start with ...`, run `test -e` on every referenced path before planner dispatch; if any path is missing, stop task creation and rewrite failure as `missing_source_file` with the missing path list. This would have prevented at least 5 wasted executions in the recent window and reduces misclassified retries on nonexistent targets to near 0.; In `planner` prompt builder, add a file-extension lock: when a task references exactly one file path, every generated step must reference that exact basename and extension, and plans that mutate `.json` to `.js` or otherwise change the extension are rejected before execution. Enforce in `validate_plan_steps()` by extracting path tokens and failing validation on any mismatch; this directly targets the repeated `incident.schema.json` -> `incident.schema.js` failures and should eliminate that failure family entirely.; In `planner` fallback logic, if the first planning attempt returns no steps or crashes in under 45s, bypass the normal planner retry and emit a deterministic 2-step micro-plan only when there is exactly 1 existing target file and the task title/body is under 220 words; otherwise classify immediately as `planning_failure`. This is enforceable in the planner wrapper and should cut the current zero-step timeout/planning waste path that appears in at least 3 recent tasks.
- 2026-03-30T11:08:51Z | SUCCESS | [self-improve:medium] Document mandatory MVP protection cases in first slice -- Start with docs/architecture/first-slice
  Rules: In `planner` task-to-step expansion, add a file-anchor lock: when the task text or `(files: ...)` declares exactly 1 path, every generated step must reference that exact basename and extension; if a step mutates `.json` to `.js` or any other path, reject and regenerate before execution. This directly targets the repeated `missing_source_file` failures on `incident.schema.json` and should eliminate that failure mode for single-file tasks.; In `orchestrator` preflight, run `test -f` on every explicit task file before step 1 and again on every step-declared path before dispatch; if the task file exists but the step path does not, fail fast as `planning_failure:path_drift` instead of spending 170-190s reaching `missing_source_file`. This reduces wasted runtime on the dominant failure category and cuts avoidable timeout exposure.; In `planner`, enforce a single-file scope template when task text matches `Start with <path>` or includes `(files: <path>)`: max 2 execution steps plus 1 verify step, and each execution step must include the target path verbatim. If the generated plan adds any second file or omits the anchored file string, regenerate automatically. This is enforceable in code and reduces both scope creep and planner-induced path drift.
- 2026-03-30T12:56:37Z | SUCCESS | [self-improve:high] Add credential recovery support to incident flow -- Start with apps/cloud-brain/src/incident-flow.mj
  Rules: In `classify_failure` and the orchestrator retry gate, reclassify any failure as `environment_validation` instead of `unknown_persistent` when `total_step_attempts <= 2`, `failed_step_index == 1`, and the failed step text starts with `Inspect`, `read`, or `identify` and there is no diff produced for the target file. This matches at least 3 recent failures (`incident.schema.json`, `first-slice.md`, `telemetry-event.schema.json`) and should prevent 3 wasted retries per 10 similar tasks.; In planner step generation, add a path-consistency precheck: if the task body contains an explicit file path with extension `.json`, `.md`, or `.mjs`, every generated step must reference that exact path string, not a sibling extension variant. Reject the plan before execution if `normalized(task_path) != normalized(step_path)`. This would have blocked the 2 repeated `missing_source_file` failures where `incident.schema.json` was incorrectly turned into `incident.schema.js`.; In verify-step synthesis, cap inline verification command length at 220 characters or 2 assertions; if exceeded, force planner to emit a repo-native validator command (`node script`, `npm test -- <target>`, or a checked-in temp script path) instead of a heredoc/one-liner. Two `unknown_persistent` failures and one `review_rejection` were attached to long embedded JSON verification commands; this rule reduces timeout and opaque parser failures on schema tasks.
- 2026-03-30T13:01:55Z | SUCCESS | [self-improve:high] Add credential recovery support to incident flow -- Start with apps/cloud-brain/src/incident-flow.mj
  Rules: In `orchestrator` failure classification, auto-reclassify `unknown` to `instrumentation_gap` when `reviewer=retry` or `evaluator=fail` but their JSON lacks a non-empty `reason_code`; do this before retry budgeting and set `retriable=false`. This targets the repeated low-signal `unknown_persistent` cases and should prevent at least 5 wasted retries from being spent on missing diagnostics.; In planner pre-check for step text generation, if a step starts with `Step 1:` and contains any of `inspect`, `read`, or `identify` and also contains `Run \`` or an inline command block, reject and regenerate the plan as a pure inspection step. This directly addresses the pattern where read-only tasks were mislabeled as `unknown_persistent` and should cut first-step false execution failures on inspection work.; In `classify_failure` or equivalent retry classifier, when the same `task_id` fails 2 times with `unknown` or `unknown_persistent` and `failed_step_index` is unchanged, force the next generated task type to `inventory_current_decision_path` scoped to exactly one file path from the original task. This is enforceable from history and should reduce repeated broad retries on the same unresolved ambiguity.
- 2026-03-30T19:01:10Z | SUCCESS | [self-improve:high] Add credential recovery trigger coverage to telemetry event schema -- Start with packages/schema/tel
  Rules: In `classify_failure` and the registry write path, ban `unknown_persistent` when `failed_step_index <= 1` or `total_step_attempts <= 2`: reclassify to `planning_failure` for planner/inspection text containing `read`, `identify`, `insertion point`, or `surrounding section`, and to `missing_source_file` when the referenced path does not exist. This would have converted at least 2 recent false-unknown failures into actionable categories and should reduce dominant-unknown noise by about 50%.; In the planner pre-check, if the task title/body targets exactly 1 file and the generated plan has more than 2 steps or includes a standalone read-only inspection step, rewrite it to `{edit step, verify step}` before execution. This matches the successful schema/doc retries, cuts wasted planner/coder/reviewer cycles on small tasks, and should shave 60-120s from single-file runs, directly lowering timeout exposure.; In `orchestrator` retry gating, if the same normalized task title plus same primary file has 2 failures within 24h and one failure is `review_rejection` or `unknown_persistent`, block another direct implementation retry and auto-generate a bounded `Inventory current decision path` task instead. This pattern already appeared for both `Add credential recovery trigger coverage...` and repeated inventory/inspection loops, and would prevent at least 2 wasted retries per repeated task family.
- 2026-03-30T20:39:24Z | SUCCESS | [self-improve:medium] Document first production-lean cloud-brain slice -- Start with apps/cloud-brain/README.md after ##
  Rules: In `orchestrator` pre-step classification, if a failure occurs on `failed_step_index=1` and the step text starts with `In \`` + `read`, `identify`, or `inspect`, remap `unknown_persistent` to `planning_failure` and persist the matched file path as evidence. This targets the repeated doc-inspection failures on `docs/architecture/first-slice.md` and should eliminate at least 2 known `unknown` misclassifications from the recent window.; In planner task generation, if the task title contains `Start with <file> after <heading>` and only one file is named, force a 2-step plan max: `edit` + `verify`; do not emit a separate inspection-only step. Enforce this in the planner plan builder for single-file markdown/doc tasks to cut the repeated step-1 inspection failures and reduce runtime by roughly 60-120s per task.; In retry policy, if the same normalized task title and same primary file fail twice with `execution_failure`, `planning_failure`, or `unknown_persistent` within 24h, block direct retry and auto-generate a narrower follow-up task capped at 1 file and 1 heading/anchor. Enforce in `orchestrator` retry scheduling to stop the observed duplicate inventory/documentation churn and prevent wasted retries on task-026/task-028 style loops.
- 2026-03-30T20:39:48Z | SUCCESS | [self-improve:medium] Document first production-lean cloud-brain slice -- Start with apps/cloud-brain/README.md after ##
  Rules: In `classify_failure` and the non-success result writer, forbid `unknown_persistent` when `failed_step_index <= 1` and the step text contains `read`, `identify`, `inspect`, `heading`, or `in \``; reclassify to `planning_failure` or `missing_source_file` and require `evidence.file_path` plus `evidence.command`. This directly targets the current dominant category and should eliminate most of the 4 recent `unknown_persistent` cases.; In the planner pre-check for single-file tasks, if the task text contains exactly one explicit file path and a positional anchor like `after ##`, `Start with`, or `in \`file\``, hard-cap the plan at 2 steps: `edit` and `verify`; reject any standalone inspection/read step. This matches the failure/success pattern on the doc tasks and cuts early-step unknown failures plus timeout exposure on bounded edits.; In `planner` task normalization, if the title/body contains `Inventory current decision path` or `bounded inventory pattern`, generate an inventory-only template with `max_steps=3`, `files_allowed` equal to the listed file paths, and `edit_budget=0`; if the draft plan includes implementation verbs (`add`, `change`, `support`, `extend`), fail plan generation and emit `scope_too_broad`. This addresses the repeated execution failures on the inventory task and prevents another implementation retry disguised as analysis.
- 2026-03-30T21:46:29Z | SUCCESS | [self-improve:medium] Document repo bootstrap decision and source of truth -- Start with docs/overview.md after ## Publi
  Rules: In `classify_failure`, auto-convert `unknown_persistent` to `planning_failure` when `total_step_attempts == 0` or `duration_seconds <= 45`; this would have caught the 30s zero-step failure on task-028 and should remove at least the planner-crash subset from the 4 recent unknowns.; In `classify_failure`, add an early-phase rule: if `failure_kind == unknown_persistent` and `failed_step_index == 1` and the failed step text contains `read`, `identify`, or `inspect` with no edit/command verb, reclassify to `planning_failure`; this targets the repeated “inspection-only step 1” pattern and prevents first-step doc tasks from being mislabeled as persistent execution failures.; In the planner pre-check, for tasks whose title starts with `Document ` and whose prompt names exactly one file plus one anchor (`after`, `near`, or `under`), hard-cap the plan at 2 steps and forbid a standalone inspection-only step; the planner must emit one edit step and one verify step. This is enforceable in plan validation and should cut the repeated step-1 unknown/planning failures on anchored doc tasks.
- 2026-03-30T21:48:06Z | SUCCESS | [self-improve:medium] Document repo bootstrap decision and source of truth -- Start with docs/overview.md after ## Publi
  Rules: In `planner/build_steps` for `self-improve` tasks that target exactly 1 file and include an explicit anchor phrase like `after ##` or `at <symbol>`, cap the plan at 2 steps and forbid an inspection-only first step when the target path is readable. This directly addresses the repeated doc-edit failures where step 1 was only “read surrounding structure,” and should cut `unknown_persistent` on anchored single-file tasks by preventing 1 wasted attempt per task.; In `classify_failure`, if `total_step_attempts == 0` or the run contains `planner failed unexpectedly`, force classification to `planning_failure` and block fallback to `unknown_persistent`. This is enforceable from existing registry/log fields and should remove misclassified zero-execution failures entirely.; In `classify_failure`, if coder, reviewer, and evaluator all contain empty or near-empty model output evidence on the same attempt (`empty_output`, blank payload, or missing `message/data` fields), classify as `empty_output` instead of `unknown_persistent`, and set `retriable=true` only once. This targets the current dominant unknown bucket by converting silent model failures into a specific class and prevents repeated opaque retries.
- 2026-03-30T21:50:32Z | SUCCESS | [self-improve:medium] Document baseline contract map for telemetry incidents and playbooks -- Start with packages/schema
  Rules: In `classify_failure` (or the retry-classifier write path), if `failed_step_index == 1`, `total_step_attempts <= 2`, the failed step text starts with `read`, `inspect`, `identify`, or `review`, and the task references exactly 1 file path, force classification to `planning_failure` instead of `unknown_persistent`. This targets the repeated step-1 inspection stalls and should convert at least the current 4 `unknown_persistent` cases into actionable planner failures.; In the planner pre-check, for tasks matching `Document|Define|Inventory` with exactly 1 referenced file and 1 anchor (`after`, `near`, `at`), reject any plan whose first step is inspection-only and regenerate a 2-step plan: `edit` then `verify`. Enforce by scanning step 1 for no edit verbs (`add|insert|update|document|extend`). This prevents the repeated single-file doc failures seen on task 028 and reduces wasted first attempts.; In `retry_decision`, if the previous result is `unknown_persistent` or `planning_failure` and the retry task title is unchanged except for wording, block the retry unless the regenerated task shrinks scope by at least one measurable dimension: fewer files, fewer steps, or removal of an inspection-only step. This prevents same-shape retries and should cut wasted retries after opaque failures.
- 2026-03-30T21:50:40Z | SUCCESS | [self-improve:medium] Document baseline contract map for telemetry incidents and playbooks -- Start with packages/schema
  Rules: In `classify_failure` or the retry classifier, if `failure_kind` is missing and `total_step_attempts == 0` or `failed_step` contains `planner failed`, `empty output`, or `planner failed unexpectedly`, force classification away from `unknown_persistent` to `planning_failure` or `empty_output`; this directly targets the 4 `unknown_persistent` cases and should cut unknown-classification waste by at least 50%.; In the orchestrator pre-retry gate, if the previous attempt was `unknown_persistent` and the next task title does not reduce scope by at least one concrete dimension (`files:` count drops, title length drops by 20%+, or conjunction count `and`/`plus`/`,` drops from >2 to <=2), block the retry and require an inventory/documentation task first; this is enforceable on task text and prevents repeated same-shape retries seen in the repeated high-scope self-improve failures.; In task creation for `[self-improve:*]` work, if the prompt names more than 1 target file or more than 2 deliverables (`add`, `document`, `inventory`, `verify`, `map`, `coverage`), auto-split into separate tasks before queueing, with each child capped at 2 execution steps; recent successful tasks were consistently 2-step, single-file edits, while the repeated failures clustered around broader high-scope tasks.
- 2026-03-30T21:56:41Z | SUCCESS | [self-improve:medium] Align credential recovery trigger coverage in account recovery playbook -- Start with packages/pla
  Rules: In `classify_failure` and the registry write path, ban `unknown_persistent` when any of `failed_step`, reviewer text, evaluator text, or raw coder output is non-empty; require a second-pass classifier over the concatenated text and emit `review_rejection`, `execution_failure`, `planning_failure`, or `timeout` instead. This targets the dominant 3-case unknown bucket and should cut unknown classifications toward 0 for non-empty evidence.; In `self-improve` task generation, reject any new task whose normalized title matches a task seen 2+ times in the last 24h unless the new title contains one narrowing token from `{inventory, document, verify, align, first slice, one file}` and references exactly 1 file path. This addresses repeated failure/retry patterns on the same credential-recovery and documentation themes and prevents wasted duplicate retries.; In `orchestrator` preflight, if a task title exceeds 160 characters or contains both `-- Start with` and `after`, force decomposition to a maximum of 2 execution steps plus 1 verify step, each step touching exactly 1 file. This is enforceable in plan validation, reduces scope creep, and directly targets timeout and review-rejection risk from overloaded self-improve prompts.
- 2026-03-30T22:03:20Z | SUCCESS | [self-improve:high] Enforce trigger-aware playbook routing in incident flow -- Start with apps/cloud-brain/src/incident-
  Rules: In `orchestrator` failure classification, add a hard override: if `coder=success` and either `reviewer` or `evaluator` produced non-empty text on both attempts, `classify_failure` must never emit `unknown_persistent`; instead re-run classification on the concatenated reviewer+evaluator+failed_step text and choose the top non-unknown label. This directly targets the 3 dominant `unknown_persistent` failures and should eliminate most false-unknown retries.; In the retry gate before step re-execution, block a second attempt when the retry has the same `failed_step_index`, same primary file path, and the retry prompt adds fewer than 20 new non-whitespace characters of failure context. Enforce this in the orchestrator retry-enrichment path and relabel as `planning_failure` instead of spending another full attempt. This cuts repeated no-change retries like task `task-044` and reduces wasted timeout budget.; In self-improve task generation, require decomposition for any task title or step text that references more than 1 file/function anchor or contains 3+ coordination verbs (`inspect`, `confirm`, `point to`, `pass`, `extend`, `verify`). Enforce in the task builder/pre-check by splitting into an inventory step and an implementation step before dispatch. This is code-checkable from task text and should reduce oversized first steps that consume 200s+ without producing differentiated failure signals.
