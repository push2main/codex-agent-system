- 2026-04-01T11:51:14Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` step generation, if a task title/body names exactly 1 writable file and contains `Start with \`<file>\`` plus `files:` with the same path, step 1 must inspect that file first and must not instruct `Inspect only spec.md`; reject and rewrite any first step that omits the named file. This would have prevented the `unknown_persistent` failure on task `task-122` and should eliminate that whole retry class for single-file smoke tasks.; In the planner pre-check, if the generated verification command does not contain at least one keyword from the task objective slug, block execution and regenerate step 3. For these smoke tasks, require overlap between task text and command flags such as `payload`, `incident`, `dashboard`, or `credential-recovery`; this would have caught the wrong `--verify-credential-recovery-routing` command on the payload-coverage task and should reduce review-rejection retries from objective/verification mismatch.; In `reviewer` or `evaluator`, add a deterministic failure classifier: if `result=SUCCESS` and `score=0`, reclassify to `review_rejection` when review text or executed command shows target-file match but objective/verification mismatch. Enforce this in the write-time classification path so low-score false successes do not hide the dominant failure mode; measurable outcome is converting current `unknown_persistent`/low-score leakage into actionable review-rejection data.
- 2026-04-01T12:53:02Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` task-to-plan generation, if a task contains exactly 1 writable file in `(files: ...)` and step 1 references any different file path, rewrite step 1 to inspect only the writable target file. This directly blocks the target-file mismatch seen in repeated `apps/cloud-brain/scripts/smoke.mjs` tasks and should prevent a large share of the 14/20 `review_rejection` retries.; In the planner fallback builder, if the prompt has `>= 3` file references but only `1` target file, keep at most `1` non-target reference in step text and move the rest into background metadata. The current fallback step copied `apps/web/README.md` into step 1 and sent the coder to the wrong file; enforcing this in the fallback path reduces review churn and keeps bounded single-file tasks under the 300s cap.; In verification-step synthesis, require lexical overlap between the task slug and the command flag: if the task title contains `incident id` or `payload coverage` and the generated verify command contains unrelated tokens like `credential-recovery-routing`, reject and regenerate the step. This is enforceable in the orchestrator before execution and prevents false-success/low-score runs where verification proves the wrong behavior.
- 2026-04-01T14:07:44Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` step generation, if a task edits exactly 1 file but the prompt names more than 2 additional reference files, rewrite step 2 to keep only the target file plus at most 1 evidence file and move the rest into hidden context. This matches the repeated `apps/cloud-brain/scripts/smoke.mjs` task family and should cut scope-driven review churn by 20%+ and prevent the same single-file task from failing as `unknown_persistent` because of overloaded step text.; In the planner that writes `failed_step`, hard-cap each step description at 420 characters or 3 backticked file paths; when exceeded, summarize with a fixed template like `Target file`, `anchor`, `expected edit`, `verification`. The only observed `unknown_persistent` failure has a visibly truncated step string (`but the smoke path still does.`), so this rule is enforceable and should eliminate prompt-truncation misfires for long self-improve tasks.; In `classify_failure`, add a rule: if any agent log contains `exec failed`, `produced no output`, `exit=1`, or `attempting failover`, classify as `provider_execution_failure` instead of `unknown`. The latest successful run still had a reviewer-side Codex crash before Claude failover, which is the same pattern that likely leaks into `unknown_persistent`; this should reduce unknown classifications immediately and preserve retry signal quality.
- 2026-04-01T15:08:36Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` task-step generation, if `task.files` contains exactly 1 file and the task title/body says `Start with <different file>` or mentions any non-listed file in step 1, rewrite step 1 to inspect only the listed file and move the extra files into non-blocking metadata; this would have prevented the `unknown_persistent` failure on task `task-122` and should eliminate this recurring mis-anchored inspect pattern entirely.; In `planner` fallback logic, when `deterministic fallback plan` is triggered and the task has `<=1` explicit writable file target, hard-cap the plan to `inspect listed file -> edit listed file -> verify requested command only if explicitly named`; forbid schema/README inspection steps unless those files are also in `(files: ...)`. Enforce in the fallback-plan builder to cut prompt drift and reduce both `unknown` failures and wasted retries on these smoke-flow microtasks.; In `classify_failure`, add a rule: if a step fails while the prompt contains `Do not read any other file unless the target file is missing` and the step target file is not in the task’s explicit file list, classify as `instruction_mismatch` instead of `unknown_persistent`; trigger this on exact substrings `Inspect only` plus a non-listed path. This converts a known planner/orchestrator defect into an actionable class and should reduce `unknown` coverage loss from 1 task now to 0 for this pattern.
- 2026-04-01T16:20:18Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` step generation, if a task’s `files:` list contains 1 writable file and the prompt says `Start with` that file, hard-block any step-1 anchor/inspection step that names a different file; rewrite step 1 to inspect only the listed file. This would have prevented the `spec.md` miss on task 122 and should eliminate at least 1 wasted retry in this recurring `smoke.mjs` task family.; In `classify_failure`, map `no such file or directory`, `rg: .*: io error`, and `not found anywhere under the repo` to `missing_dependency` when the missing path is absent from the task `files:` list; do not leave these as `unknown`. This is enforceable with regex on coder/reviewer/evaluator text and should reduce unknown classifications for file-targeting errors to near 0.; Add a preflight validator in `orchestrator` before dispatch: for every explicit file path mentioned in a generated step, run `test -e`; if the path is missing and the task has a single writable file, auto-rewrite the step to the writable file instead of executing. This converts environment/path drift into a 1-second precheck and avoids the current 47-72s wasted on doomed attempts.
- 2026-04-01T17:16:55Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-check, if a task targets exactly 1 file and the title/body names the same file path 2+ times or repeats the same noun phrase stem seen in the last 10 project-local tasks, collapse the prompt to `goal + exact file + exact assertion target` and strip all secondary references after the first 60 words; enforce before planner dispatch. This addresses the repeated `apps/cloud-brain/scripts/smoke.mjs` verification pattern and should cut oversized-plan fallbacks and reduce timeout exposure on these 200-400s single-file tasks.; In `planner` task splitting, when `word_count >= 60` or coordination markers/conjunctions `>= 4`, do not emit a generic 3-step flow. Instead force a `2-step` template for single-file tasks: `inspect named file only` then `edit + verify`. Only allow a separate verification step if the command is explicitly provided and estimated runtime is `< 30s`. This is enforceable in planner prompt generation and should remove one full coder/reviewer/evaluator cycle per repeated smoke task, reducing timeout risk and wasted retries.; In `classify_failure` and the run writer, if `result=SUCCESS` and `score=0`, require ingestion of reviewer + evaluator text and emit a concrete non-unknown subtype from `{verification_missing, scope_drift, weak_assertion}` before persisting. If no subtype can be assigned, write `classification_reason_missing` and fail the classification pass. The dominant weakness is still `unknown`; this rule prevents low-signal outcomes from feeding `unknown_persistent` and should improve actionable retry routing on every ambiguous run.
- 2026-04-01T19:27:45Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` step-generation for self-improve tasks, when the task title contains a quoted file target and a specific contract token like ``incident_id`` or ``payload coverage``, require step 3 verification text to contain that same token or the same task slug substring; otherwise regenerate the verification step before execution. This would have prevented the mismatched `--verify-credential-recovery-routing` verification command on the incident-id task and should cut low-score false-success/`unknown_persistent` cases for smoke assertions by at least 1 task per repeat cycle.; In `post-guard` or `classify_failure`, reclassify `SUCCESS` + `score<=0` + `reviewer=approved` + `evaluator=success` as `verification_mismatch` when the final verification command text does not overlap the task title by at least 2 normalized keywords. This is enforceable with a token-overlap check and is better than leaving these cases as `unknown_persistent`, improving unknown classification coverage on low-score successes.; In the self-improve task generator, block creation of another implementation task for the same normalized title when the last 3 runs on that title were `SUCCESS` within 12 hours and the only delta is wording around the same file (`apps/cloud-brain/scripts/smoke.mjs`). Force generation of exactly one `inventory` or `gap-analysis` task instead. The history shows repeated verify/inventory/verify loops on the same smoke file; this rule reduces retry churn and unnecessary 200-350s runs.
- 2026-04-01T21:31:49Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-queue normalization, if the normalized scope key matches a `SUCCESS` in the last `600` minutes and the touched-file set is identical or a subset, block new implementation tasks and emit a diagnostic-only inventory task instead; this directly targets the repeated `apps/cloud-brain/scripts/smoke.mjs` churn pattern and should prevent at least 3 duplicate executions per recurring scope.; In plan generation, if a task title/body contains exactly `1` file target and phrases like `one focused assertion` or `smallest safe change`, hard-cap the plan to `2` steps (`edit`, `verify`) and skip the separate anchor-discovery step; enforce in the planner fallback path to cut the repeated 3-step overhead seen across these smoke-flow tasks and reduce timeout exposure on sub-300s jobs.; In `orchestrator` verification synthesis, require token overlap between the task contract and the verify command: at least `2` shared normalized keywords from the task-specific noun set (`incident_id`, `dashboard`, `payload`, `credential_recovery_trigger`, etc.); if overlap `< 2`, reject the plan and regenerate. This would have caught the misaligned latest verify command `--verify-credential-recovery-routing` on an `incident_id` task and converts likely `unknown_persistent` outcomes into deterministic plan errors.
- 2026-04-01T23:36:43Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` step synthesis, add a scope-token validator that extracts the top 3 nouns/file stems from the task title and required files, then rejects or rewrites any generated step whose instruction text overlaps fewer than 2 of those tokens; this would have caught the stale Step 3 command (`credential-recovery-routing`) on an `incident_id` task and should reclassify such runs as `unknown_scope_mismatch` instead of `unknown`, improving unknown coverage on repeated smoke-flow tasks.; In the planner fallback path, if the task prompt is marked oversized and names exactly 1 writable file target, force a 2-step plan only: `inspect target file` and `edit+verify target file`; skip template-derived cross-file inspection steps unless the target file is missing. Enforce this in the fallback planner where `task prompt has >=4 coordination markers` currently triggers, and cap total planned steps at 2 for single-file tasks to cut timeout exposure by roughly 30-40% on the recurring `smoke.mjs` assertion tasks.; In reviewer/evaluator result ingestion, add a hard classification rule: when `result=SUCCESS` and `score<=0`, persist `soft_success_low_confidence` instead of leaving the run effectively opaque; if the same task family hits this state 2+ times in 20 runs, enqueue a verification-focused follow-up rather than a generic retry. This targets the observed `Low score (0) despite SUCCESS` pattern and converts current unknown/ambiguous evidence into a measurable class.
- 2026-04-02T01:41:47Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` task synthesis, if a task title/body references exactly 1 writable file and contains `Add one`, `one focused assertion`, or `smallest safe change`, force the plan to exactly 3 steps: `inspect target file -> edit target file -> run one verification command`; reject any planner output that adds cross-file inspection beyond 2 read-only references. This codifies the pattern seen across repeated `apps/cloud-brain/scripts/smoke.mjs` tasks and should cut timeout exposure on narrow tasks by keeping them under the existing 300s cap.; In the task generator/deduper, if the same normalized objective appears 2+ times within the last 24 hours with the same primary file target, do not enqueue another `inventory current decision path` variant unless the previous attempt ended as `timeout` or `missing_dependency`; otherwise merge it into the existing task or suppress it. This directly targets the repeated `inventory -> verify` loop around `apps/cloud-brain/scripts/smoke.mjs` and prevents wasted retries on already-successful scopes.; In `reviewer` result validation, if the reviewer returns `approved` while the evaluator score is `0` or the post-guard emits `Low score despite SUCCESS`, rewrite the final classification to `verification_gap` instead of `success` and require one explicit artifact in the reviewer JSON: changed file path plus exact verification command plus pass/fail text. This is enforceable in the review/eval aggregator and converts currently hidden review-rejection debt into a retryable, well-classified state.
- 2026-04-02T03:47:03Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` task-scope pre-check, if a prompt exceeds `60` words or contains `>=4` coordination markers/conjunctions, rewrite it to a 3-field template `{target_file, single edit intent, single verification command}` and strip all secondary references beyond the first `2` files; the latest task hit `70` words and `4` conjunctions, and enforcing this cutoff should reduce timeout exposure on the same prompt shape that already triggers scope warnings.; In task creation for `self-improve` items, add a duplicate-suppression gate keyed by `{project, primary_file, normalized intent}` over the last `10` completed tasks: if there are `>=2` `SUCCESS` runs in that window and no intervening `FAILURE`, block new retries/inventory tasks for `30` minutes; the recent history repeats the same `apps/cloud-brain/scripts/smoke.mjs` verify/inventory cycle 9+ times, so this would cut wasted work and prevent low-value retries from consuming timeout budget.; In `reviewer` result validation, require every `review_rejection` to include a machine code from a fixed enum plus at least one file reference; if either is missing, reclassify to `unknown_unusable_review` and block automatic retry generation. With `14/20` recent retries caused by `review_rejection`, this rule targets the dominant failure mode and prevents retries driven by non-actionable reviewer output.
- 2026-04-02T05:51:58Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-queue dedupe, block any new task whose normalized intent + target file matches a `SUCCESS` from the same project within the last 12 hours and whose latest result had `attempts=1`; only allow it through if the new task payload contains a different required symbol or file target. This would have suppressed the repeated `apps/cloud-brain/scripts/smoke.mjs` verify/inventory loop seen 9+ times and cuts review-rejection churn from redundant work.; In `post-guard` and task generation, treat `SUCCESS` with `score <= 0` as `needs_spec_followup`, not as an implementation retry trigger: write a follow-up only if reviewer/evaluator text contains a concrete missing assertion/file reference; otherwise do not enqueue another code task. Enforce in `create_followup_task` with a regex requiring at least one path and one missing-behavior phrase, which should prevent a large share of the 14 recent `review_rejection` retries caused by non-actionable low-score outcomes.; In `classify_failure` for `review_rejection`, downgrade to `non_actionable_review` and stop retries when the review text lacks both a file path and a change verb (`add|remove|assert|rename|guard|validate|test`); after 1 such downgrade, require regeneration of the task prompt instead of rerunning coder/reviewer. This is code-enforceable, improves error classification, and prevents repeated retries on vague reviews.
- 2026-04-02T07:59:06Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `self-improve` task generation, suppress any new task whose normalized title plus target file list exactly matches a `SUCCESS` from the same project in the last 24 hours unless a newer failed run mentions that same file. This pattern appears repeatedly for `apps/cloud-brain/scripts/smoke.mjs`; enforcing it would cut duplicate low-yield executions and reduce avoidable timeout exposure from repeated verify/inventory loops.; In the orchestrator pre-check, if a task description contains both `Start with` and more than 2 backticked file paths, rewrite it to keep only the file(s) listed in the explicit `(files: ...)` suffix and drop the rest into non-executable context notes. Recent tasks succeeded as single-file edits while still carrying 3-4 file references; this rule reduces scope inflation and should lower timeout risk on coder/reviewer passes.; In reviewer dispatch, when the active step is verification-only and the coder output reports no file diff plus exactly 1 command execution, skip reviewer entirely or hard-cap reviewer runtime at 15s in `orchestrator`/`run_reviewer_step`. The latest run hit a reviewer timeout on step 3 despite a simple smoke command, so this is a direct timeout-prevention rule for a repeated class of wasted review work.
- 2026-04-02T10:13:47Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` preflight, if `task.files` resolves to exactly 1 file and the normalized instruction contains `Add one`, `one focused assertion`, or `smallest safe change`, hard-cap the plan to 2 steps (`implement`, `verify`) and skip inventory/research steps. This would have removed the wasted third step on `apps/cloud-brain/scripts/smoke.mjs` and should cut timeout exposure for this recurring pattern by roughly 25 to 35%.; In task generation for `self-improve` work, if the same normalized intent and primary file have both succeeded within the last 24 hours at least 2 times, block creation of another implementation task and allow only a `verification-gap` task when there is concrete failure evidence. Enforce this in the task creator before enqueue. This targets the repeated `Verify dashboard incident id field in smoke flow` / `Inventory current decision path` loop and prevents duplicate scope churn.; In `classify_failure`, if `reviewer=retry` and `evaluator=fail` but the reviewer/evaluator text contains none of: a file path, line reference, test name, command, or quoted failing assertion, classify as `review_rejection_unactionable` instead of `unknown`, and suppress automatic retry. This is enforceable with regex checks on the combined texts and would reduce wasted retries from the current dominant review-rejection bucket.
- 2026-04-02T12:50:43Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` plan assembly, force any self-improve task whose `files:` list contains exactly 1 file and whose title/body mentions `Add one focused assertion`, `one deterministic assertion`, or `smallest safe change` into a hard 2-step plan: `implement` then `verify`. Reject planner outputs with 3+ steps for these tasks. This targets the repeated `apps/cloud-brain/scripts/smoke.mjs` assertion tasks that keep running as 3-step jobs and should cut wall time by roughly 25-35%.; In the task-scope gate, add a rule for `project=superheld`: if a task references the same primary file and normalized intent as any success in the last 12 hours and the previous durations for that intent are `>240s` in at least 2 runs, rewrite the next task into `inventory-only` or `verify-only` before enqueueing. This matches the repeated `verify dashboard incident id field` and `inventory current decision path` loop and prevents another full implementation retry from consuming 300s+.; In `orchestrator.sh` or the equivalent step runner, ban reviewer and evaluator phases on inventory artifacts written under `codex-memory/self-improve-*.md` when the step text includes `Do not implement code changes in the same run`. Auto-mark those steps as `coder-only + schema check`. The latest successful inventory task spent substantial time on reviewer/evaluator despite being a bounded artifact write; removing those phases should save 20-40s per inventory step and reduce timeout exposure.
- 2026-04-02T15:06:34Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope gating, if `task.files` resolves to exactly 1 file and the title/body contains any of `Verify`, `Inventory`, or `smoke flow`, force `max_plan_steps=2` and reject planner outputs with 3+ steps; this matches at least 11 recent single-file self-improve tasks that all ran the same 3-step shape and should cut timeout exposure by roughly one third.; In `planner` fallback generation for self-improve tasks, if the prompt already names a primary file and an anchor symbol/path segment (for example `apps/cloud-brain/scripts/smoke.mjs` plus `dashboardIncidentFields` or `credentialRecoveryRun`), emit `implement+verify` only and suppress any separate inspection step unless the previous attempt failed with `missing_dependency` or `unknown`; this prevents repeating the inventory-before-edit pattern that consumed full runs before the timeout on task 167.; In `orchestrator` preflight, if a self-improve task text exceeds 45 words or contains 2+ backticked file references, rewrite the execution payload to keep only the primary file plus at most 1 supporting file reference before sending to coder/reviewer/evaluator; recent timeout-prone prompts for the same smoke task were 59-word multi-reference prompts, so this is an enforceable context cap aimed at reducing timeout rate on verbose single-file work.
- 2026-04-02T17:18:40Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In the orchestrator pre-check for `self-improve` tasks, if the task body names exactly 1 writable file and contains both `Start with` and `Add one deterministic assertion` or `Add one focused assertion`, force a 2-step plan only: `edit target file` and `run one verification command`; skip planner-generated inventory/research steps. This addresses the repeated single-file smoke assertions that still ran as 3-4 steps and should cut timeout exposure by roughly 25-35% on this task shape.; In task-scope gating, add a hard splitter for verbose single-file tasks: if word count is `>45` and the task references only 1 file in the `(files: ...)` list, rewrite the execution prompt to keep only the target file plus at most 2 supporting paths extracted from backticks; drop README/spec/schema references from coder context unless they are also writable targets. Recent timeout and near-timeout runs repeatedly carried extra docs into `apps/cloud-brain/scripts/smoke.mjs` work; this should reduce oversized-context timeouts and prevent wasted review cycles.; In `planner` fallback logic, if a task title starts with `Verify ` and the latest 3 matching tasks for the same normalized file target have median duration `>=280s`, prohibit adding an `inventory` step unless the previous attempt failed with `review_rejection` or `missing_dependency`; for `timeout`, require direct scope reduction inside the implementation step instead. The history shows repeated alternation between `Verify ... smoke flow` and `Inventory current decision path ...` on the same file, which adds work without fixing the timeout path.
- 2026-04-02T19:27:22Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope pre-check, if `task.word_count > 45` and `files` contains exactly 1 path, rewrite the task to a compact form capped at 30 words: keep only target file, anchor symbol, and one expected assertion/command. The latest inventory run was 59 words and already triggered timeout reduction; enforce prompt compression before dispatch to cut single-file timeout risk on this repeated smoke-flow pattern.; In `planner.buildPlan`, if the task title starts with `Inventory current decision path` or contains `bounded inventory pattern`, force a 2-step plan only: `inspect_and_write_inventory`, `verify_named_command`. Reject any extra research/implementation step generation. This pattern appeared at least 6 times on the same `apps/cloud-brain/scripts/smoke.mjs` thread and consistently succeeded in 210-244s; keeping it at 2 steps avoids timeout drift from unnecessary planner expansion.; In `planner.buildPlan`, if `files.length == 1`, the task body names an anchor symbol like ``dashboardIncidentFields`` or ``credentialRecoveryRun``, and the requested change is “one focused assertion” or “one deterministic assertion”, force a 2-step `implement`, `verify` plan and ban inventory subtasks unless the immediately previous attempt on the same normalized title failed. This exact assertion-style smoke-flow pattern recurred 8+ times; adding inventory after a timeout increases cycle time without adding new scope information.
- 2026-04-02T21:42:50Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope precheck, if `task_word_count >= 45` and `files:` lists exactly 1 file, rewrite the execution brief down to `primary_file + one anchor symbol + one required assertion/change + one verify command`, and drop README/schema/spec references from the coder prompt unless they are the target file. This directly matches the repeated `apps/cloud-brain/scripts/smoke.mjs` tasks (59-word inventory task, long verify task) and should cut single-file timeout risk by reducing prompt size before step 1.; In planner fallback logic for self-improve tasks, if the normalized task title prefix repeats within the last `8` project-local tasks and the target file is unchanged, force a `2-step` plan only: `implement_or_inventory` and `verify`; do not emit a separate inspection step. The `verify dashboard incident id` and paired `inventory current decision path` tasks recur 7+ times against the same file, and the extra inspect phase is wasted latency that increases timeout exposure on otherwise bounded work.; In `orchestrator` retry handling, when `failure_kind=timeout` on a task with `files_count=1`, `total_step_attempts <= 3`, and `failed_step` contains `implement the smallest safe change`, auto-generate a reduced retry task capped at `35` words and hard-limit the plan to one edit site plus one verification command. This prevents repeating the broad implementation prompt that timed out on task-167 and should eliminate at least one wasted retry per small-file timeout.
- 2026-04-03T02:23:34Z | SUCCESS | [self-improve:critical] Fix value measurement blindness -- Start with `apps/cloud-brain/scripts/smoke.mjs`. Recent tasks
  Rules: Keep prompt changes minimal and tied to repeated evidence.; Prefer prompt rules that improve determinism and verification.; Avoid task-specific prompt tweaks unless the same failure repeats.
- 2026-04-03T04:39:19Z | SUCCESS | [self-improve:critical] Fix value measurement blindness -- Start with `apps/cloud-brain/scripts/smoke.mjs`. Recent tasks
  Rules: Keep prompt changes minimal and tied to repeated evidence.; Prefer prompt rules that improve determinism and verification.; Avoid task-specific prompt tweaks unless the same failure repeats.
- 2026-04-03T06:54:37Z | SUCCESS | [self-improve:critical] Fix value measurement blindness -- Start with `apps/cloud-brain/scripts/smoke.mjs`. Recent tasks
  Rules: Keep prompt changes minimal and tied to repeated evidence.; Prefer prompt rules that improve determinism and verification.; Avoid task-specific prompt tweaks unless the same failure repeats.
- 2026-04-04T20:11:31Z | FAILURE | Add graceful error recovery to queue-worker.sh for orphaned lease cleanup
  Rules: Keep prompt changes minimal and tied to repeated evidence. Prefer rules that improve determinism and verification.; When review_rejection occurs (44% of retries), diff coder output against reviewer objection and inject a targeted fix hint into the retry prompt.; Tasks touching more than 3 files per step should be split — file-count correlates with timeout and step_not_completed failures.
- 2026-04-06T12:14:56Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-check, if `failure_kind=timeout` occurred with `total_step_attempts=0` for the same normalized title family at least 2 times, force `planner_mode=deterministic_fallback` and skip provider planning on the next retry. Trigger on titles with `word_count >= 12` or `coordination_markers >= 2`. This directly targets the repeated 60s planner timeouts seen in `task-012`, `task-013`, `task-014`, and `task-015` and should eliminate most zero-step timeouts.; In the task scope gate, if a task title contains `for all`, `across`, or more than 1 explicit file/module target, rewrite it into a bounded first task touching exactly 1 file or 1 script plus 1 verification step. Enforce this in the task generator before queueing. This addresses the repeated coder timeouts on `Add shellcheck validation step...`, `Extract repeated JSON manipulation patterns...`, and `Add unit tests...` by cutting broad multi-surface work into single-surface retries.; In `orchestrator.sh` step budgeting, if a title family has 2+ timeout failures and the generated plan has more than 2 implementation steps, collapse it to `inspect -> edit -> verify` with a hard max of 3 total steps. Reject longer plans before execution. This is enforceable from the plan JSON and should reduce per-step budget exhaustion like the repeated step-2/step-3 coder timeouts in `task-012`, `task-014`, and `task-015`.
- 2026-04-06T15:16:06Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` preflight, auto-split any coding step whose prompt contains more than 220 words or more than 2 file-path literals into `inspect-anchor` and `edit-target` substeps before dispatch; this directly targets the repeated `Step 2 coder timed out` pattern seen on `task-012`, `task-014`, `task-015`, and `task-187` and should reduce per-step timeout exposure by keeping implementation prompts bounded.; In the planner/orchestrator task builder, if a task title starts with `Add ` or `Extract ` and the description implies both code change and validation work, enforce a maximum of 1 file owner or 1 artifact type per execution task; otherwise rewrite into separate tasks at creation time. This is enforceable in the task-generation pre-check and addresses the repeated multi-scope timeouts on `Add shellcheck validation...`, `Add unit tests...`, and `Extract repeated JSON...`.; In `orchestrator` timeout handling, if `total_step_attempts == 0` or the planner times out before execution (`duration_seconds <= 70` and `failed_step` contains `Planner timed out`), do not retry the same task body; instead auto-generate a bounded inventory task capped to 1 target file and 120 prompt words. This prevents wasted retries on the same oversized prompt pattern seen in three separate zero-step timeouts (`task-012`, `task-013`, `task-015`).
