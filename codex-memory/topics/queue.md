- 2026-03-26T12:18:16Z | FAILURE | [self-improve:low] Drain approval backlog -- 12 active approvals are waiting (12 approved). Review pending approvals, pa
  Rules: In `agents/planner.sh`, reject any task title or step prompt over 24 words or containing 3 or more coordination markers (`and`, `with`, commas, parentheses) unless the planner first rewrites it into a maximum 3-step plan with one file target per step; this directly targets the repeated zero-step timeouts on 30-word multi-clause tasks and should cut planner timeout exposure before execution starts.; In `agents/planner.sh` and `agents/orchestrator.sh`, enforce a hard pre-step budget split: planner must emit step 1 within 20s, full plan within 45s, and abort at 60s with a deterministic JSON handoff task containing only the first bounded step; 23 recent timeouts were planner-dominated, so this rule prevents another full-worker-budget loss when planning stalls.; In `agents/planner.sh`, add a repository-grounding precheck for inspection tasks: if the prompt names variables/functions/files, the planner must run a fast symbol existence scan and rewrite missing identifiers out of the step before dispatch; if 2 or more named identifiers are absent, classify as `scope_mismatch` instead of sending to coder. This would have prevented the `pending_approval_tasks`/`approved_tasks` miss that burned 2 attempts on `scripts/multi-queue.sh`.
- 2026-03-29T22:48:47Z | SUCCESS | [self-improve:low] Drain approval backlog -- Start with scripts/verify-baseline.sh at the top-level verification block i
  Rules: In `agents/planner.sh`, reject any generated step longer than 450 characters or containing more than 2 backticked path/anchor references, and regenerate once with a one-file/one-edit template. This targets the repeated `review_rejection` pattern from over-specified step text and should cut reviewer retries on planner-generated tasks that currently fail before coding.; In `agents/planner.sh`, if a task title starts with `[self-improve:` and is longer than 110 characters or contains both `--` and a metric phrase like `%`, `rate`, or `analyze`, force decomposition to a single concrete file target before step generation; otherwise emit `scope_too_broad` and do not queue. This addresses the 4 repeated broad self-improve failures and should reduce zero-step and step-2 timeout churn.; In `scripts/lib.sh` preflight validation, when a step mentions a specific file plus an anchor phrase (`immediately after`, `around`, `line`, `comment`, or quoted identifier), require `test -f` on the file and `rg -F` for the anchor before coder execution; if either check fails, classify immediately as non-retriable `missing_source_file`. This prevents the repeated wasted second attempts seen in at least 5 tasks and should save 1 retry per affected task.
- 2026-03-29T23:51:58Z | SUCCESS | [self-improve:critical] Inventory current decision path for cap pre-step planning budget -- Start with `packages/schema/
  Rules: In task-generation code for self-improve prompts, add a file-extension consistency guard: if `files:` names `*.json` but any generated step body references the same basename with a different extension (for example `incident.schema.js` vs `incident.schema.json`), reject the plan before execution and regenerate the step text from the canonical `files:` entry. Enforce in the planner/orchestrator pre-check that every referenced path must exactly match one declared file path; this directly targets the repeated `missing_source_file` failures on `packages/schema/incident.schema.json` and should eliminate that entire class of basename-drift retries.; In orchestrator step validation, require a filesystem existence check for every backticked or linked path before coder dispatch; if any path is absent, stop at step 0 with `missing_source_file` and mark the step non-retriable after 1 identical miss. Store a normalized fingerprint of the missing path set in retry classification so the same bad step text cannot run twice. This is enforceable in the pre-execution validator and would have prevented at least the 2 repeated `incident.schema.js` misses plus other repeated missing-file retries.; In planner prompt construction, add a hard cap for self-improve task bodies: if task title + rationale + injected evidence exceed 900 characters, rewrite the task into an inventory-only step with exactly 1 file and 1 expected artifact path before planning. Enforce in the self-improve task synthesizer and planner input builder. This targets the 60s zero-step planner timeouts and the 1-2s planning failures by reducing prompt size before the planner runs.
- 2026-03-30T07:40:47Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `scripts/orchestrator.sh` pre-plan validation, hard-cap self-improve tasks to `<=3` steps and `<=220` total task-title words; if the title exceeds either limit, auto-split into an inventory task plus one implementation task. This targets the repeated 333s, 345s, 353s, 414s, and 532s runs and should cut timeout exposure on oversized single-file schema tasks.; In the planner prompt builder for self-improve tasks, if `files:` contains exactly 1 path ending in `.json` or `.schema.json`, forbid any plan step from naming a sibling `.js` file unless that file is also in the declared file list; enforce with a regex guard before dispatch and fail as `missing_source_file_plan`. This directly blocks the repeated `incident.schema.json` -> `incident.schema.js` drift that caused 2+ failures and avoids wasting full attempts on impossible steps.; In `classify_failure` and the post-step verifier, reclassify any failure whose `failed_step` starts with `Step 3 (verify):` and whose command contains `python - <<`, `node -e`, or heredoc execution as `verification_script_failure` instead of `unknown_persistent` or `review_rejection`. This fixes the repeated misclassification on schema verification tasks, improves retry routing, and prevents retries from reusing the same broken verification path.
- 2026-03-30T09:54:24Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `scripts/orchestrator.sh` preflight, auto-convert any `files:` entry ending in `.schema.json` into a hard single-file scope and reject plans that mention more than 1 file or any sibling extension swap such as `.js`; this targets the repeated `missing_source_file` pattern on `incident.schema` inventory tasks and should prevent at least 2 wasted retries per normalized task key.; In the planner step builder, if the normalized task title starts with `Inventory current decision path` and the allowed file count is 1, force a 3-step template: `inspect file`, `summarize decision points`, `verify summary exists`; reject any generated plan over 3 steps or over 450 prompt words. This directly reduces timeout/execution failures seen across the repeated inventory tasks and should cut long-running inventory attempts below 300s.; In `classify_failure`, map any failure where `failed_step` contains `verify` or the last step index and the command includes `python - <<'PY'`, `node -e`, `assert`, or `json.load` to `verification_failure` instead of `unknown_persistent`; this addresses the 2 repeated schema verify failures currently hidden as unknown and prevents low-value retries on misclassified verification errors.
- 2026-03-30T12:09:13Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `build_step_prompt` for single-file inventory/self-improve tasks, hard-lock every generated file reference to the exact path from the task’s `(files: ...)` metadata or first explicit backticked path; if any later step swaps `.json` to `.js` or introduces a different basename, abort before execution and classify `missing_source_file`. This directly targets the repeated `incident.schema.json` -> `incident.schema.js` drift seen in 2 failures and should prevent at least those wasted retries.; In orchestrator preflight, run `test -f` on every explicit task file before planner dispatch when `task` contains `Start with`, `(files: ...)`, or `Inspect only`; if any path is missing, fail in under 5s with `missing_source_file` and skip coder/reviewer/evaluator. This converts the dominant 5 `missing_source_file` cases from 170-190s runs into immediate exits and cuts timeout exposure.; In `normalize_task_scope`, if a task title/body contains `Inventory current decision path` or `Document ... after ##`, force a 3-step template only: `inspect target`, `write inventory/doc artifact`, `verify artifact exists`; reject any generated plan step containing `implement`, `code changes`, or multi-file exploration. This is enforceable at plan validation and addresses the repeated execution/planning drift that failed twice before the same inventory task later succeeded with the bounded pattern.
- 2026-03-30T14:22:39Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `classify_failure` and the failure writer, ban `unknown_persistent` when `failed_step` contains `Step N (verify): Run` or `Step 1: In \`` and a command/file path is present: parse the command text and stderr/stdout, then remap to `verification_failure`, `json_parse_failure`, `assertion_failure`, `missing_source_file`, or `inspection_failure`; if remap confidence is still low, persist `unknown_needs_evidence` and attach the raw command string. This directly targets the 4 recent `unknown_persistent` failures and should cut unknown-classified retries by at least 50%.; In the task planner pre-check, if a self-improve task title starts with `Add ` and the task body references exactly 1 file, force a 2-step plan only: `edit` then `verify`; move any “inspect current shape/format/insertion point” text into the edit step prompt instead of creating a standalone discovery step. This is enforceable in planner generation and prevents the repeated Step-1 inspection failures seen in both schema/doc tasks.; In `orchestrator` retry control, if the same `task_id` fails twice with the same `failed_step_index` and the same normalized verify command hash, block a third full retry and auto-split into a bounded inventory task plus one implementation retry candidate. This addresses the repeated schema-task loops on identical verify steps and should reduce wasted retries on small-file tasks by at least 1 run per repeated failure cluster.
- 2026-03-30T16:36:59Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `orchestrator` pre-plan task shaping, if a task title/body contains `Start with` or `Inspect` and references exactly 1 file path, force a 2-step plan maximum (`inspect-or-edit`, `verify`) instead of generating a standalone read-only Step 1. This directly targets the repeated `unknown_persistent` failures on step-1 inspection tasks (`incident.schema.json`, `first-slice.md`) and should cut 1 wasted step per affected task and reduce timeout exposure by roughly 25 to 35%.; In `classify_failure`, if `failure_kind` would be `unknown_persistent` and `failed_step_index == 1` and the failed step text starts with `Step 1:` plus contains `inspect`, `read`, or `identify`, automatically relabel to `planning_failure` unless the coder log contains a concrete shell/runtime error. This is enforceable against the 3 repeated first-step inspection failures and prevents low-signal `unknown` labels from blocking the right remediation path.; In `planner` fallback generation for self-improve tasks, if the prompt includes `Inventory current decision path` or `bounded inventory` and the task has exactly 1 target file, emit the fixed 3-step template that succeeded here: `anchor-only inspect`, `write one inventory artifact`, `test -s artifact`. Do not allow extra exploratory steps or multi-file expansion unless a second file is explicitly named. This converts a previously failing family into a deterministic path and reduces retry churn on task family `Inventory current decision...`.
- 2026-03-30T18:51:32Z | SUCCESS | [self-improve:high] Inventory current decision path for add credential recovery trigger coverage to telemetry event sche
  Rules: In `orchestrator` pre-check, if a self-improve task targets exactly 1 file and the title/body contains `Start with` or a concrete path, collapse any leading read-only inspection step into the edit step and cap the plan at 2 steps (`implement`, `verify`); this removes the repeated inspect-first pattern seen in `task-025` and `task-028`, and should cut single-file run time by about 25 to 35 percent to reduce timeout exposure.; In `classify_failure`, never leave a failure as `unknown_persistent` when `failed_step_index <= 1` and `failed_step` text contains `read`, `inspect`, `identify`, `after ##`, or `in \``; reclassify to `planning_failure` if `total_step_attempts=0`, otherwise to `execution_failure_inspection`. This directly targets the 4 dominant unknown failures and should eliminate most of that bucket.; In planner fallback generation, if the task text includes `Inventory current decision path` or `bounded inventory` and names 1 file, force the deterministic 3-step inventory template (`anchor scan`, `write inventory artifact`, `test -s artifact`) and reject any plan containing `implement`, `modify schema`, or more than 3 steps. This codifies the successful pattern from `task-026/027/029/033/034/035` and prevents the earlier execution failures on the same inventory family.
- 2026-03-30T22:22:20Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In `classify_failure` and the retry write-path, ban `unknown_persistent` when `failed_step` contains a concrete path in backticks or a command string longer than 20 chars: first run `test -e` on every referenced file and map missing paths to `missing_source_file`, and scan command text for `docker`, `playwright`, or `run-*-docker.sh` to map to `sandbox_restriction` or `missing_dependency`. This would have reclassified at least 2 recent `unknown_persistent` failures and prevents wasted generic retries.; In the orchestrator pre-check, reject any task whose verify step includes an executable command and a file path not present in the repo (`rg --files` miss on any backticked path or final command token ending in `.sh|.mjs|.js`). Mark the task `blocked_invalid_verification` before step 1 instead of letting coder/reviewer run. This directly targets the 2 recent `missing_source_file` failures and should cut one full failed attempt cycle per task.; In task generation for `self-improve`, force an inventory-first rewrite when the title starts with `Enforce`, `Add`, or `Verify`, the task touches exactly 1 file, and the same normalized title has failed once in the last 24h. Replace the implementation task with a 3-step bounded inventory task capped at 1 primary file and 3 total steps. The latest success shows this pattern works; enforcing it should reduce repeat `unknown_persistent` retries on first-step inspection tasks.
- 2026-03-30T23:26:37Z | SUCCESS | [self-improve:high] Inventory current decision path for add baseline verification for dashboard payload contract fields 
  Rules: In `orchestrator` task pre-check, parse every backticked path plus every `(files: ...)` entry from the task/step text, run `test -e` on each before step creation, and abort the task as non-retriable `missing_source_file` if any referenced path is absent. This would have prevented both `task-043` and the first `task-045` retry from reaching execution.; In `orchestrator.build_verification_steps`, if a verify command references any script under `tests/` or `scripts/` that fails the same `test -e` preflight, rewrite the task state to `blocked_missing_verifier` and skip coder/reviewer/evaluator dispatch entirely; enforce this specifically for shell commands extracted from `Run \`...\`` step text. Target outcome: eliminate wasted retries on frozen verification commands with missing local files.; In `classify_failure`, if evaluator/reviewer/coder text contains a missing-path signal (`ENOENT`, `No such file`, `not found`, `cannot access`) and the missing token resolves to a repo-relative path, force `failure_kind=missing_source_file` and `retriable=false` even when another label like `sandbox_restriction` is present. This would have stopped the second `task-045` run from being misclassified after the same missing verifier command.
- 2026-03-30T23:35:32Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident payload coverage in smoke flow -- Dire
  Rules: In `orchestrator` preflight, parse every verification command for repo-local path tokens matching `(^|/)(scripts|tests|apps|packages)/[^ "'\`]+` and run `test -e` on each before dispatch; if any path is missing, fail the step immediately as `missing_source_file`, mark non-retriable, and skip coder/reviewer/evaluator. This directly targets the repeated `bash scripts/run-playwright-docker.sh bash tests/dashboard-screenshot-verification.sh` failures and should eliminate that retry waste entirely.; In task creation and approval code, validate every path named in `Start with ...`, `(files: ...)`, and inline shell commands before a task can enter the queue; if any referenced file is absent, reject the task with `missing_source_file` instead of generating execution work. Enforce this in the task normalizer so the same bad task text cannot be re-approved under a new run id.; In `classify_failure`, add a normalized-command signature for verification steps; if the same command fails 2 times within 24h with `missing_source_file` or `sandbox_restriction`, auto-shelve further retries for that command and emit a bounded inventory/environment task instead. This is enforceable against the repeated Playwright Docker verification command and prevents multi-attempt churn.
- 2026-03-31T00:27:52Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In `orchestrator` task preflight, parse every backticked shell command in step text and run `command_path_exists()` on each repo-relative script token before queueing or approval; if any path like `scripts/run-playwright-docker.sh` or `tests/dashboard-screenshot-verification.sh` is missing, hard-fail as `missing_source_file` and mark non-retriable. This directly targets the 2 repeated missing-file failures and should prevent those retries entirely.; In `approve_task` and `planner.build_steps`, reject any verify step whose command references a file outside the task’s declared `files:` list unless that file exists and is whitelisted in a new `verification_dependencies` field; enforce with a repo-path count check on step text. This would have blocked the `apps/web/README.md` task from carrying an undeclared Playwright dependency into execution.; In `classify_failure`, add precedence: if stderr or failed_step contains `No such file`, `cannot access`, `not found`, or a referenced repo path fails `fs.existsSync`, classify as `missing_source_file` even when sandbox or timeout text is also present. This prevents the same broken verify command from splitting across `missing_source_file` and `sandbox_restriction`, improving retry decisions for at least 2 recent failures.
- 2026-03-31T01:40:12Z | SUCCESS | [self-improve:high] Inventory current decision path for add baseline verification for dashboard payload contract fields 
  Rules: In `orchestrator` preflight, parse every backticked or repo-relative path from `task`, `failed_step`, and verification text, and reject the task before planner dispatch if any referenced file under the repo is missing; emit `failure_kind=missing_source_file`. This would have prevented both `task-043` and the first `task-045` retry from spending 85s and 267s on impossible verification.; In the task builder that emits verification steps, add a command-path validator: for every shell token matching `scripts/*.sh`, `tests/*`, `apps/*`, or `packages/*`, require `test -e` on the referenced path before the step is written; if validation fails, replace the task with an inventory-only follow-up and tag `blocked_by_missing_file`. This targets the repeated bad step `bash scripts/run-playwright-docker.sh bash tests/dashboard-screenshot-verification.sh`.; In retry classification/write-time relabeling, if the same task title fails once with `missing_source_file`, block any retry whose plan still references the same missing path set; mark it `non_retryable_same_missing_inputs` instead of requeueing. This would have prevented the second `task-045` run and cuts wasted retries in the dominant failure pattern.
- 2026-03-31T03:58:01Z | SUCCESS | [self-improve:high] Inventory current decision path for add baseline verification for dashboard payload contract fields 
  Rules: In `orchestrator` task-expansion logic for "Add or update one focused failing or currently missing regression test" steps, require that at least one path from the parent task’s `(files: ...)` list appears in the generated child step text; if `projects/superheld/spec.md` is injected but absent from the parent file list, block step creation and classify `missing_source_file`. This targets the repeated dashboard-baseline failures where retries drifted from `scripts/verify-baseline.sh` to a nonexistent spec path and should prevent at least the 3 known missing-file failures.; Add a preflight in the step builder that extracts every backticked or repo-relative path from `failed_step`, `verification command`, and `files:` metadata, resolves them against repo root, and aborts before coder execution when any explicit path is missing; implement in the same pre-dispatch validation path that currently checks task text. Threshold: if `missing_paths >= 1`, skip retries and write `failure_kind=missing_source_file`. This reduces wasted retries on invalid inputs and should save 1 full attempt for each bad task.; In `classify_failure`, upgrade `unknown` to `missing_source_file` when reviewer/evaluator text contains both a missing-path signal (`no such file`, `does not exist`, `cannot find`, `ENOENT`) and an extracted repo-relative path; do this before the generic unknown fallback. The latest successful inventory run still burned a retry because step 1 attempt 1 was logged as `unknown` despite path-anchoring discussion; this rule improves classification coverage and prevents at least 1 wasted retry per similar case.
- 2026-03-31T06:10:32Z | SUCCESS | [self-improve:high] Inventory current decision path for add baseline verification for dashboard payload contract fields 
  Rules: In the orchestrator pre-step builder, reject any generated step whose instruction text introduces a repo-relative path not present in the task’s declared `files` list or original prompt paths; validate with a repo-path regex plus `test -e` before execution, and rewrite the step instead of running it. This would have prevented the `projects/superheld/spec.md` detour seen in at least 3 `missing_source_file` failures and should cut that category materially.; In the retry planner, add a same-missing-input lock: if attempt `n` failed as `missing_source_file` and attempt `n+1` still references the same nonexistent path set, skip the retry and emit `non_retriable_missing_source_file`. Enforce this in retry-decision code by hashing sorted missing paths from classifier output; this prevents 1 wasted retry per occurrence and reduces timeout exposure from doomed reruns.; In step-generation code, cap path fan-out for self-improve tasks: if the task declares exactly 1 explicit file, generated implementation or test-writing steps may reference at most 2 repo-relative paths total; otherwise force an inventory step before any edit/test step. Enforceable with a regex count on unique paths in the step text; this targets repeated scope-creep failures where a single-file task expanded into unrelated files.
- 2026-03-31T09:10:35Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident payload coverage in smoke flow -- Dire
  Rules: In `planner/build_steps` and `planner/expand_spec_step`, if a generated step introduces any repo path not present in the task’s `files:` list or a prior existence-checked artifact table, run `test -e` on that path before accepting the step; on the first miss, regenerate once with the missing path removed, and on the second miss, classify immediately as `missing_source_file`. This directly targets the 2 recent `missing_source_file` failures and should prevent wasted coder/reviewer/evaluator cycles on nonexistent files.; In `orchestrator` before step execution, add a `validate_step_paths(step_text)` gate that extracts backticked and plain repo-like paths and rejects the step if more than 1 unchecked path appears in a single-file task. Apply this specifically to verify steps and spec-edit steps. This reduces scope creep from tasks like `scripts/verify-baseline.sh` work that later drifted into `projects/superheld/spec.md` or missing scripts.; In `classify_failure`, add a hard rule: if the failed step contains `No such file`, `not found`, or references a repo path that fails `test -e` before any command starts, force `failure_kind=missing_source_file` and set `retry_allowed=false` for normal retries. This preserves the current 100% retry classification coverage while preventing repeated retries on deterministic path errors.
- 2026-03-31T09:21:15Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In orchestrator step-generation preflight, reject any step whose `paths[]` are not a subset of the task-declared `(files: ...)` list plus files existence-checked in an earlier completed step; when violation count `>=1`, force plan regeneration before coder execution. This directly targets the 2 recent `missing_source_file` failures and should prevent those retries entirely.; In planner fallback for `self-improve` implementation tasks, if the task title contains `Start with` and exactly 1 file path, emit step 1 as `test -e <that file>` plus anchor discovery, and block step 2/3 creation until that check passes. Enforce this in the deterministic planner path used by inventory follow-ups; measurable outcome: converts missing-path failures into zero-cost preflight exits instead of wasting a full attempt.; In verify-step synthesis, only allow commands from a per-task verifier whitelist built from files found by `rg --files` during step 1; if a verify command references a script not in that whitelist, replace it with a file-local verification command or route back to replanning. This addresses both recent `missing_source_file` failures where verification referenced unavailable scripts and should eliminate that failure mode in single-file tasks.
- 2026-03-31T11:18:16Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident payload coverage in smoke flow -- Dire
  Rules: In `orchestrator` preflight, when a task includes `(files: ...)` or a single backticked primary path, run `test -e` on every declared path before step generation; if any path is missing, abort before planner execution and classify `missing_source_file`. This directly targets the 2 recent `missing_source_file` failures and prevents wasting all 3 planned steps on impossible work.; In planner task normalization, if the prompt names exactly 1 primary file but the generated plan references `>=2` additional repo paths or any verify command touching other files, force step 1 to be an inventory/existence-check step that enumerates those dependencies before implementation. Enforce this in the planner fallback path used for self-improve tasks; expected outcome is fewer late-step `missing_source_file` failures from hidden dependencies like `scripts/verify-baseline.sh`.; In `classify_failure`, add a deterministic override: if the failed step text contains `Run \`bash ` or `Run \`node ` and stderr/stdout includes `No such file`, `cannot open`, `MODULE_NOT_FOUND`, or `ENOENT`, classify as `missing_source_file` instead of `missing_dependency` or `unknown`. This should keep retry coverage at 1.00 while reducing wasted retries on file/path problems.
- 2026-03-31T11:28:38Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In `orchestrator` preflight, parse every `files:` path from the task text and every literal command path from planned verify steps; if any referenced file does not exist before step 1, abort planning and classify `missing_source_file`. This would have prevented both recent `missing_source_file` failures from reaching verify.; In `planner` task normalization, when a task’s primary edit file and verify command touch different roots (for example `packages/schema/...` edit plus `scripts/verify-baseline.sh` verify), inject a mandatory dependency-check step that runs before implementation and records existence of all verify artifacts. Enforce this in the plan builder for any task whose verify step mentions a path not listed in `files:`.; In `orchestrator` scope gate, if task text exceeds 45 words and contains both `Start with` plus 2 or more backticked file paths, do not only reduce timeout; rewrite the run into an inventory-first task or split into `inventory` then `implement`. The one recent timeout happened at 59 words and already triggered the verbose gate, so the current behavior is too weak.
- 2026-03-31T13:43:53Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, parse every backticked path plus every path inside the task `(files: ...)` suffix, run `fs.existsSync` before step creation, and fail the task immediately with `classification=missing_source_file` if any referenced path is absent. This extends the current explicit-file guard to all task-supplied path channels and should eliminate the repeated 2/2 missing-file retries before execution starts.; In the planner fallback used for self-improve tasks, reject any planned verification command whose primary script/file argument does not exist in the repo root at plan time; write `missing_source_file` into the step record instead of emitting a runnable step. Target the planner function that converts task text into `Run ...` verification steps, and prevent wasted coder/reviewer/evaluator passes on nonexistent smoke or baseline targets.; In retry gating, add a hard block in `classify_failure` or retry admission: if the last failure is `missing_source_file` and no newer commit modified the missing path’s nearest existing parent directory, set `max_retries=0`. This is enforceable from git diff plus the stored missing path, and it prevents repeated retries when the repository state has not changed.
- 2026-03-31T15:59:15Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, parse every path from both backticked text and the `(files: ...)` suffix; if the task references `>=2` unique repo paths and any one does not exist at dequeue time, stop before planning, classify as `missing_source_file`, and set `retryable=false`. This hardens the existing missing-file rule against prompt/body drift and should eliminate the current 2/2 missing-source retries.; In the planner fallback used for `"Inventory current decision path"` tasks, if the title contains `Inventory current decision path` or `bounded inventory`, force `max_steps=2` and ban executable verification steps. Enforce this in the deterministic-plan builder, because these inventory tasks have appeared repeatedly and the latest run still spent a third step on an unrelated smoke command; removing that step cuts unnecessary runtime by about 25 to 35 seconds per inventory task and reduces timeout exposure.; In command synthesis for verification steps, require lexical alignment between the generated CLI flag and the task slug: normalize both strings and require at least `2` shared non-stopword tokens, or require one exact referenced file/function token match. If the check fails, do not run the command; classify as `planner_mismatch` instead of `timeout` or generic failure. This directly addresses the latest mismatch where an `incident id` inventory task ran `--verify-credential-recovery-routing`.
- 2026-03-31T18:13:35Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, if a task title contains `"(files:"`, extract every listed path and run `fs.existsSync` on each before planning; if any path is missing, stop before step 1, classify as `missing_source_file`, and set `retryable=false`. This turns the current dominant failure mode into a zero-step failure and prevents wasted planner/coder/reviewer time.; In `planner` fallback generation, if the task text matches `Inventory current decision path` or `bounded inventory pattern`, force a 2-step plan only: `inspect/write inventory artifact` and `verify artifact exists`; block any execution step containing `node `, `npm `, `pnpm `, or test/smoke commands for those tasks. The latest successful inventory task still spent a third step on an unrelated smoke run, which is avoidable timeout surface.; In the task-scope gate inside `orchestrator`, when task word count is `> 45` and the task already declares exactly one writable file in `(files: ...)`, cap the plan at `max_steps=3`, `max_refs=3`, and require every generated step to mention that file path or the declared artifact path. This is enforceable with a step-text validator and reduces timeout risk from verbose but actually narrow tasks.
- 2026-03-31T20:28:09Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task title starts with `Inventory current decision path for` or contains `Do not implement code changes in the same run`, force a 2-step template only: `inspect` and `write inventory artifact`; block any third `verify` step unless the command string contains the exact primary file from `(files: ...)`. This removes off-target verification like the latest run’s `--verify-credential-recovery-routing` step and should cut wasted review/eval cycles on inventory tasks by 1 step per run.; In planner step generation, if task text is longer than 45 words or contains both `Start with` and `Add deterministic assertion`, auto-split into a bounded single-file task plus a separate follow-up verification task, and cap each generated step description at 30 words. The project already shows a 31% timeout rate and repeated verbose smoke-flow tasks; this rule is enforceable before execution and should reduce timeout exposure on long self-improve tasks.; In `reviewer` output validation, reject any review marked `approved` when evaluator score is `0` or `<50` unless the review JSON includes `cause`, `evidence_path`, and `verification_match=true`. Recent history has at least 5 SUCCESS tasks with scores `0/31/32/65/66`, which is a repeated misclassification pattern; forcing structured justification will improve the dominant `review_rejection` retry class and prevent low-signal approvals from being treated as clean successes.
- 2026-03-31T22:42:36Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task is single-file (`files:` has 1 path) and the title/body references the same target file plus 2 or more secondary contract files, rewrite it into a 2-step bounded task: `inventory` then `implement`, and cap writable scope to the single target file only. This addresses the repeated `apps/cloud-brain/scripts/smoke.mjs` review rejections caused by contract-validation tasks expanding beyond one concrete edit site.; In `build_retry_prompt` and `classify_failure`, when `failure_kind=review_rejection` on a single-file task, require the retry payload to include all 3 fields before requeue: `target_file`, `exact_edit_anchor`, and `rejection_delta` extracted from reviewer text; if any field is missing, generate an inventory task instead of a direct implementation retry. This is enforceable at queue time and should cut wasted review-rejection retries in the dominant category.; In `task_deduper` or strategy generation, if 2 or more open/recent tasks within 12 hours target the same file and share a normalized stem of at least 5 words after removing verbs like `verify`/`inventory`, suppress new implementation tasks and emit at most one follow-up verification task. The history shows the same 3 smoke-flow themes being regenerated repeatedly; this rule reduces scope churn and prevents redundant retries.
