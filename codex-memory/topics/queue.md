- 2026-03-31T22:42:36Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task is single-file (`files:` has 1 path) and the title/body references the same target file plus 2 or more secondary contract files, rewrite it into a 2-step bounded task: `inventory` then `implement`, and cap writable scope to the single target file only. This addresses the repeated `apps/cloud-brain/scripts/smoke.mjs` review rejections caused by contract-validation tasks expanding beyond one concrete edit site.; In `build_retry_prompt` and `classify_failure`, when `failure_kind=review_rejection` on a single-file task, require the retry payload to include all 3 fields before requeue: `target_file`, `exact_edit_anchor`, and `rejection_delta` extracted from reviewer text; if any field is missing, generate an inventory task instead of a direct implementation retry. This is enforceable at queue time and should cut wasted review-rejection retries in the dominant category.; In `task_deduper` or strategy generation, if 2 or more open/recent tasks within 12 hours target the same file and share a normalized stem of at least 5 words after removing verbs like `verify`/`inventory`, suppress new implementation tasks and emit at most one follow-up verification task. The history shows the same 3 smoke-flow themes being regenerated repeatedly; this rule reduces scope churn and prevents redundant retries.
- 2026-04-01T00:58:00Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-admission logic, if a new task’s normalized title matches a task that had `review_rejection` or `SUCCESS` with `score <= 32` in the last `6h`, block direct implementation and rewrite it to `verification` or `inventory` first. This pattern appears repeatedly across the three smoke-flow scopes and should prevent at least 1 wasted retry per repeated scope while cutting review-driven churn.; In the planner pre-check for single-file tasks, if `files:` contains exactly `1` file but the prompt references `>= 2` additional backticked contract files or modules, require an explicit anchor tuple `{primary_file, function_or_array_name, assertion_site}` before step 2 can be an implementation step. Enforce this in the planner fallback path used for `apps/cloud-brain/scripts/smoke.mjs`; it directly targets the repeated review-rejection pattern caused by “one file to edit, multiple external contracts to reconcile.”; In the task scope gate inside `orchestrator`, if a self-improve prompt is `> 45 words` or contains both `Start with` and `>= 2` backticked dependency paths, auto-compact it to one edit target and one expected assertion, or split it into at most `2` queued tasks before execution. The current verbose-task timeout reduction to `450s` is reactive; this rule is enforceable earlier and should reduce both timeout exposure and reviewer rejections on oversized prompts.
- 2026-04-01T03:13:13Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-shaping pre-check, if a self-improve task targets exactly 1 file and the prompt body exceeds 45 words, auto-rewrite it to `{target file} + {single anchor symbol} + {one required assertion/change} + {one verification command}` before planner dispatch; this matches the recent slow/rejected smoke tasks (59-word inventory prompt, 318s rejected implementation) and should cut review-driven ambiguity and reduce verbose-task timeout pressure.; In `planner` fallback generation, when the task title contains `Verify` and the same normalized task family had any `review_rejection` or `score <= 32` in the last 10 project-local runs, force a 2-step plan only: `edit` then `run named verification command`; reject any extra inspect/inventory step unless the title explicitly contains `Inventory`; this prevents wasting one full step on already-understood single-file smoke assertions and should remove 1 redundant step from the recurring `verify dashboard incident payload coverage` loop.; In `post-guard` and retry gating, treat `SUCCESS` with `score = 0` on `self-improve` tasks as `verification_missing` instead of plain success, and block new tasks in the same normalized family until the result record includes a non-empty verification artifact or command output fingerprint; this is enforceable in result classification and addresses the repeated pattern of low-score successes immediately followed by more inventory/verify churn.
- 2026-04-01T05:27:24Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope gating, if `files:` lists exactly 1 file and task text is longer than 45 words or contains 3 or more backticked path/function references, rewrite the coder/reviewer prompt to `goal + target file + one anchor + one verification command` before step 1. This directly targets the repeated `review_rejection` pattern on single-file smoke tasks and should cut prompt-scope rejections without changing task intent.; In `planner` fallback generation, if the task title starts with `Verify ` and the target file is `apps/cloud-brain/scripts/smoke.mjs`, force a 2-step plan only: `edit assertion` and `run one smoke command`; reject any generated plan that inserts inventory, broad code-path inspection, or more than 2 references. This is enforceable in the plan validator and reduces the review failures caused by over-scoped plans on these repeated smoke-flow tasks.; In retry classification and task creation, when `failure_kind=review_rejection` for a task touching a single file, auto-tag the retry as `scope_reduction_required` and require the next retry prompt to be at least 30% shorter than the failed prompt text; block requeue if length reduction is not met. This prevents wasting retries by resubmitting the same verbose shape and should convert repeated review rejections into one bounded retry.
- 2026-04-01T07:44:05Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` plan synthesis, if `task.files` contains exactly 1 path and the title/body contains `Add one`, `one focused assertion`, or `one deterministic assertion`, hard-cap the generated plan to 3 steps and reject any step that mentions more than 2 repo paths. This directly reduces the review-rejection pattern caused by scope drift on single-file smoke tasks and should cut wasted retry attempts on that class.; In the pre-dispatch validator for step text, if a step for a single-file task asks to inspect a different file first (for example `spec.md`) before the declared target file, rewrite the step to inspect the declared file first and move supporting-file reads to the same step only when explicitly cited by the task body. This is enforceable in planner/orchestrator and addresses the repeated failure mode where bounded implementation tasks get derailed into anchor-hunting.; In `classify_failure`, when `failed_step_index == 1`, `task.files` has length 1, and the failed step contains phrases like `Inspect only`, `identify the narrowest`, or `Expected: name`, auto-classify as `scope_mismatch` instead of `unknown_persistent`. This improves retry routing for the observed inspection-stage failures and prevents repeating the same oversized review-sensitive prompt.
- 2026-04-01T09:57:10Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope pre-check, if `files:` lists exactly 1 path and the title/body mention exactly 1 concrete edit goal, force a `single_file_strict` plan template with only 3 steps: inspect target file, edit target file, run one task-aligned verification command. Reject any generated step that references another source file unless it is read-only evidence named in the task. This should cut review rejections from out-of-scope exploration and reduce timeout exposure on the repeated `apps/cloud-brain/scripts/smoke.mjs` tasks.; In `planner` fallback-plan generation, detect inventory-only follow-ups by title patterns `^Inventory current decision path` or body text `Do not implement code changes in the same run`; when matched, forbid any verification command that executes unrelated smoke modes. Require step 3 to validate only the inventory artifact exists and mentions `primary file`, `edit location`, and `decision points`. This prevents review rejection from mismatched verification and avoids wasted runtime on non-edit tasks.; In `reviewer` and `classify_failure`, add a deterministic `verification_mismatch` failure kind: trigger when the final verification command or reviewer notes mention a flag/command not present in the task objective keywords, especially for smoke tasks under `apps/cloud-brain/scripts/smoke.mjs`. Auto-classify these instead of `unknown_persistent`. This would have converted the 2026-04-01 payload-coverage failure into a reusable signal and improves retry targeting.
- 2026-04-01T12:12:14Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-shape detection, if a task names exactly 1 writable file and includes exactly 1 implementation verb from `{add, verify, assert, include, inventory}`, force a 3-step template: `inspect target file -> edit or inventory only -> run one named verification command`. Enforce by bypassing freeform planner output for this shape; expected outcome is fewer review rejections from plan drift and lower timeout risk on the large single-file retry cohort.; In planner step generation, reject any step-1 anchor that points to a file not listed in the task when the task already includes a concrete target file. If `step_1.primary_file != task.files[0]`, regenerate the plan once and log `target_file_mismatch`. This directly addresses repeated smoke-flow tasks where success came from starting in the named script and failure came from detouring into broader spec inspection.; In `classify_failure`, map failures to `review_rejection` when reviewer/evaluator text shows any of these broad signals together: `wrong file`, `no requested assertion/change`, `inventory instead of implementation`, or `verification did not prove requested contract`. Apply this at write time to replace `unknown_persistent`; expected outcome is eliminating the remaining unknown-class failures in this recurring single-file assertion pattern and preventing wasted generic retries.
- 2026-04-01T14:28:26Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `planner` task normalization for `self-improve` prompts, if the task targets exactly 1 writable file and the title/body exceed 45 words, rewrite step 1 to `inspect <primary file> at <anchor>` and move every other reference (`spec.md`, schema/playbook files, rationale text) into metadata only. Enforce before plan generation in the orchestrator pre-check. This should cut prompt size on the repeated `apps/cloud-brain/scripts/smoke.mjs` task family and reduce timeout/unknown failures from instruction overload.; In `planner` step synthesis, if an inspection step contains `Inspect only` plus a file that is not in the task’s explicit `(files: ...)` list, block the step and regenerate it against the listed primary file. Enforce in the function that emits step text by validating step file targets against parsed task metadata. This directly prevents the `unknown_persistent` miss seen when step 1 drifted to `spec.md` instead of `apps/cloud-brain/scripts/smoke.mjs`.; In `classify_failure`, add a deterministic `instruction_mismatch` class when a failed step is inspection-only and the coder output omits either the required anchor/file name or introduces edits despite `Do not modify`. Trigger when `failed_step` contains `Inspect`, `Expected:`, or `report the anchor`, and coder/reviewer JSON lacks the expected field. This converts current `unknown`/`unknown_persistent` cases into actionable labels and should eliminate most of the remaining unknown-class retries.
- 2026-04-01T16:41:21Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In planner pre-check for single-file tasks, if the task text contains `Start with \`<path>\`` and the generated first step does not mention that exact path, reject and regenerate the plan once before execution; implement in the planner step builder/validator. This directly addresses the 2026-04-01 `unknown_persistent` failure where step 1 incorrectly targeted `spec.md` instead of `apps/cloud-brain/scripts/smoke.mjs`, and should eliminate that whole failure class for repeated smoke-flow tasks.; In `classify_failure`, map step-1 failures to `planning_scope_drift` instead of `unknown` when the failed step references a file outside the task’s `files:` list or outside the explicit `Start with` path set; use exact string/path matching against task metadata and failed-step text. This is enforceable and improves retry routing/classification coverage by preventing planner mistakes from landing in `unknown_persistent`.; Add an orchestrator guard: if a task title repeats within the last 10 project-local tasks and prior successful runs for that title used `total_step_attempts <= 3`, force the fallback 3-step template `inspect target file -> apply bounded change or inventory artifact -> verify exact command` and block ad hoc planner expansion. The same smoke-flow families recur 2+ times here and consistently succeed with the bounded template; enforcing reuse should cut timeout exposure and prevent wasted retries.
- 2026-04-01T18:56:40Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `classify_failure` and the post-run writer, add a hard rule: if `result=SUCCESS` but `score <= 0` or `post-guard` logs `Low score ... flagging task`, write `classification=review_rejection` when reviewer status is not `approved`, else `classification=unknown_persistent_low_score`; never leave these cases as plain `unknown`. This is enforceable from existing fields and should eliminate the current 1-task unknown bucket while improving retry routing for all low-score successes.; In `strategy-loop` task generation, if the same normalized title stem appears `>= 2` times in the last `12h` for the same primary file and all runs are `SUCCESS`, block any new `Inventory current decision path ...` task and require the next task type to be `implementation` or `verification` only. The recent history shows repeated inventory tasks on `apps/cloud-brain/scripts/smoke.mjs`; this prevents scope-stagnation loops and cuts wasted retries on already-mapped code paths.; In `orchestrator` pre-check, when a task title contains both `Inventory current decision path` and `Do not implement code changes`, force a 2-step path only: `inspect -> write artifact`; reject any added verification command step such as `Run node apps/cloud-brain/scripts/smoke.mjs ...`. The latest successful inventory run still executed 3 steps including a smoke command, which inflates duration without changing outcome; this should reduce timeout exposure on inventory-only tasks by roughly one third.
- 2026-04-01T21:02:18Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task pre-check, if a task title contains `Inventory current decision path` or `inventory-only` and the same normalized scope has a `SUCCESS` in the last 6 hours, force a single-step run (`inspect -> write artifact`) and skip planner-generated verification/commit steps; this removes redundant step 3 executions and should cut 1-2 unnecessary model rounds per repeat inventory task.; In `orchestrator` scope gating, if `files:` contains exactly 1 file and the task body includes both `Start with` and `Add one` or `Add deterministic assertion`, hard-cap the plan at 3 steps with exactly 1 editable file and exactly 1 verification command; reject plans that introduce secondary implementation targets unless the reviewer cites a blocking dependency. This directly reduces timeout exposure on the repeated `apps/cloud-brain/scripts/smoke.mjs` pattern.; In `planner` or step synthesis, when the task slug mentions one contract (`incident id field`, `payload coverage`, `credential recovery routing`) and the generated verification command does not contain the same normalized keyword family, fail plan generation and require regeneration. The latest inventory run verified `credential-recovery-routing` while analyzing `incident id field`; this rule prevents misclassified `SUCCESS` and should reduce `unknown`/`unknown_persistent` outcomes caused by wrong-command evidence.
- 2026-04-01T23:16:56Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` step-builder for `self-improve` tasks, if the title contains `Inventory current decision path` or `Do not implement code changes in the same run`, force a 2-step template only: `inspect` + `write inventory artifact`; block any third `run smoke` verification step. Enforce in the plan assembly function by checking task text before step generation. This removes irrelevant command execution from inventory-only runs and should cut 15-25% of avoidable timeout surface on diagnostic tasks.; In the planner/orchestrator handoff, add a task-to-verification keyword match guard: extract 3-5 normalized keywords from the task slug and require the verification command/artifact path to share at least 2 of them. If not, reject the step and replan. Apply this in the function that emits step 3 commands. This would have caught `verify dashboard incident id field` being verified with `--verify-credential-recovery-routing`, preventing unknown/low-evidence “success” outcomes and reducing misclassified retries.; In `classify_failure` and the post-run reviewer aggregation, if `result=SUCCESS` but `score<=0` or `post-guard` emits `Low score despite SUCCESS`, write classification `evidence_gap` instead of leaving it under `unknown_persistent`. Implement by parsing combined evaluator+reviewer+post-guard text before final registry write. This is directly enforceable and should convert repeated unknown failures into a retryable class with better follow-up prompts, likely eliminating most of the remaining `unknown` bucket in this project.
- 2026-04-02T01:22:35Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task title shares the same normalized target file and at least 4 identical content words with any `SUCCESS` task from the last 20 project-local tasks, rewrite the new task to `inventory-only` and cap it to 3 steps (`inspect -> write artifact -> verify existing command`). This prevents repeated implementation/review churn in the same scope and should cut the dominant `review_rejection` retries on near-duplicate smoke tasks.; In `planner` and `orchestrator`, hard-fail plan generation when a single-file task (`files:` contains exactly 1 path) produces more than 3 steps or any step text over 35 words; auto-rewrite to the minimal template `inspect -> edit/inventory -> verify`. The recent successful pattern is consistently 3 steps, while all-time timeout rate is 0.27, so enforcing this shape should reduce long-tail timeout exposure.; In `reviewer` output validation, require one of exactly four machine-checked outcomes in the JSON/message contract: `approved`, `request_changes`, `missing_dependency`, or `timeout_risk`, and reject any review that lacks a file path plus one concrete failing condition. Wire this check where reviewer results are saved so ambiguous reviews are reclassified immediately instead of becoming `review_rejection` on retry; this directly targets the 14/20 dominant retry classification.
- 2026-04-02T03:38:00Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `task creation` or `queue enqueue` for `project_local`, if the normalized task title shares the same primary file and the same first 8 stemmed words with a `SUCCESS` task from the last 24 hours, suppress the new task unless the prior task had `score < 0` or a different `required_edit`. This directly targets the repeated `apps/cloud-brain/scripts/smoke.mjs` verify/inventory loop seen 10+ times and should prevent wasted reviewer cycles on duplicate follow-ups.; In `orchestrator` step planning, if a task contains `Inventory current decision path`, `bounded inventory`, or `Do not implement code changes`, force a 2-step max workflow: `inspect/write artifact` and `verify artifact exists`; block any code-run step such as `node ...smoke.mjs`. The latest inventory task still spent 3 steps and 236s, so this rule reduces timeout exposure and cuts unnecessary execution on inspection-only work.; In `reviewer` output validation and `classify_failure`, reject any `review_rejection` without one required reason code from a fixed enum like `wrong_file`, `missing_assertion`, `verification_missing`, `scope_violation`, `artifact_missing`, `test_regression`; if absent, auto-reclassify to `unknown_unusable_review` and do not retry the same implementation path. Review rejections are the dominant retry class (14/20), and this prevents opaque reviewer feedback from causing churn.
- 2026-04-02T06:03:34Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task pre-check, reject any new task whose normalized title matches a task that succeeded in the same project within the last `12h` and touches the same `(files: ...)` set, unless the prompt includes either `failure evidence:` or `regression:`. This directly targets the repeated `Inventory current decision path...` / `Verify dashboard incident id...` loops seen 8+ times and should prevent wasted successful duplicates and follow-up churn.; In `planner.build_execution_path`, force `inventory-only` tasks to a `2-step` max path: `inspect -> write artifact`; forbid any verification command step when the task text contains `Inventory current decision path`, `bounded inventory`, or `Do not implement code changes`. The latest run still executed a smoke command unrelated to the inventory target; removing that third step cuts runtime by roughly `25-35%` for this recurring pattern and reduces timeout exposure.; In `review_rejection` retry generation, add a hard gate in `create_followup_from_review`: if reviewer feedback lacks both `1` concrete file path and `1` explicit requested edit verb (`add`, `remove`, `assert`, `guard`, `rename`, `update`), classify as `non_actionable_review` and do not enqueue a retry. With `14/20` recent retry failures coming from review rejection, this prevents vague reviews from spawning churn.
- 2026-04-02T08:20:38Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-planning, collapse repeated self-improve intents when the normalized title and primary file match any SUCCESS in the last 10 project-local tasks; block new task creation unless the new task adds a different file target or a newer failure timestamp. This would have prevented the 10-task alternation on `apps/cloud-brain/scripts/smoke.mjs` and cuts duplicate review load that is feeding `review_rejection`.; In the self-improve generator, if a task prompt contains `Inventory current decision path` and `Do not implement code changes in the same run`, hard-cap the plan to 2 steps and forbid any runtime verification command in step 3+. The latest inventory run still executed `node apps/cloud-brain/scripts/smoke.mjs --verify-credential-recovery-routing`, adding ~90s of avoidable work; this rule directly reduces timeout exposure on inventory-only tasks.; In `reviewer` or retry-task synthesis, reject any retry seed whose review text lacks both a concrete file path regex match and a concrete action verb (`add|remove|assert|rename|guard|mock|validate|update`). Last-20 retries are dominated by `review_rejection` (14/20); this gate converts vague review loops into no-op/non-retry outcomes and prevents wasted retries from non-actionable reviews.
- 2026-04-02T10:35:15Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-enqueue, block duplicate follow-up tasks when `normalized_title + primary_file` already succeeded within the last `6h` and no newer failing artifact exists for that same pair. Enforce by comparing the newest `FAILURE` timestamp or failing command/assertion after the last `SUCCESS`; if none exists, classify as `duplicate_followup_suppressed` and do not queue. This should cut the current inventory/verify churn on `apps/cloud-brain/scripts/smoke.mjs` and prevent repeated no-op retries.; In the planner path for single-file tasks, if `files` count is `1` and the task text contains any of `Add one focused assertion`, `deterministic assertion`, or `bounded inventory`, force a `2-step` plan only: `implement_or_inventory`, `verify`. Do not generate a separate inspect step. Enforce in plan builder before step expansion. This directly targets the observed `3-step` runs taking `166s-331s` and should reduce timeout exposure on the same task family.; In `classify_failure`, tighten review rejection gating: if reviewer output does not contain at least one concrete locator matching a file path, line number, shell command, test name, or assertion text, reclassify from `review_rejection` to `unactionable_review` and set `retryable=false`. Apply this in the reviewer/evaluator write path, not only on retry. With `14/20` recent retries classified as `review_rejection`, this is the highest-value rule to stop wasted retries.
- 2026-04-02T12:50:43Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` plan assembly, force any self-improve task whose `files:` list contains exactly 1 file and whose title/body mentions `Add one focused assertion`, `one deterministic assertion`, or `smallest safe change` into a hard 2-step plan: `implement` then `verify`. Reject planner outputs with 3+ steps for these tasks. This targets the repeated `apps/cloud-brain/scripts/smoke.mjs` assertion tasks that keep running as 3-step jobs and should cut wall time by roughly 25-35%.; In the task-scope gate, add a rule for `project=superheld`: if a task references the same primary file and normalized intent as any success in the last 12 hours and the previous durations for that intent are `>240s` in at least 2 runs, rewrite the next task into `inventory-only` or `verify-only` before enqueueing. This matches the repeated `verify dashboard incident id field` and `inventory current decision path` loop and prevents another full implementation retry from consuming 300s+.; In `orchestrator.sh` or the equivalent step runner, ban reviewer and evaluator phases on inventory artifacts written under `codex-memory/self-improve-*.md` when the step text includes `Do not implement code changes in the same run`. Auto-mark those steps as `coder-only + schema check`. The latest successful inventory task spent substantial time on reviewer/evaluator despite being a bounded artifact write; removing those phases should save 20-40s per inventory step and reduce timeout exposure.
- 2026-04-02T15:06:34Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope gating, if `task.files` resolves to exactly 1 file and the title/body contains any of `Verify`, `Inventory`, or `smoke flow`, force `max_plan_steps=2` and reject planner outputs with 3+ steps; this matches at least 11 recent single-file self-improve tasks that all ran the same 3-step shape and should cut timeout exposure by roughly one third.; In `planner` fallback generation for self-improve tasks, if the prompt already names a primary file and an anchor symbol/path segment (for example `apps/cloud-brain/scripts/smoke.mjs` plus `dashboardIncidentFields` or `credentialRecoveryRun`), emit `implement+verify` only and suppress any separate inspection step unless the previous attempt failed with `missing_dependency` or `unknown`; this prevents repeating the inventory-before-edit pattern that consumed full runs before the timeout on task 167.; In `orchestrator` preflight, if a self-improve task text exceeds 45 words or contains 2+ backticked file references, rewrite the execution payload to keep only the primary file plus at most 1 supporting file reference before sending to coder/reviewer/evaluator; recent timeout-prone prompts for the same smoke task were 59-word multi-reference prompts, so this is an enforceable context cap aimed at reducing timeout rate on verbose single-file work.
- 2026-04-02T17:18:40Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In the orchestrator pre-check for `self-improve` tasks, if the task body names exactly 1 writable file and contains both `Start with` and `Add one deterministic assertion` or `Add one focused assertion`, force a 2-step plan only: `edit target file` and `run one verification command`; skip planner-generated inventory/research steps. This addresses the repeated single-file smoke assertions that still ran as 3-4 steps and should cut timeout exposure by roughly 25-35% on this task shape.; In task-scope gating, add a hard splitter for verbose single-file tasks: if word count is `>45` and the task references only 1 file in the `(files: ...)` list, rewrite the execution prompt to keep only the target file plus at most 2 supporting paths extracted from backticks; drop README/spec/schema references from coder context unless they are also writable targets. Recent timeout and near-timeout runs repeatedly carried extra docs into `apps/cloud-brain/scripts/smoke.mjs` work; this should reduce oversized-context timeouts and prevent wasted review cycles.; In `planner` fallback logic, if a task title starts with `Verify ` and the latest 3 matching tasks for the same normalized file target have median duration `>=280s`, prohibit adding an `inventory` step unless the previous attempt failed with `review_rejection` or `missing_dependency`; for `timeout`, require direct scope reduction inside the implementation step instead. The history shows repeated alternation between `Verify ... smoke flow` and `Inventory current decision path ...` on the same file, which adds work without fixing the timeout path.
- 2026-04-02T19:27:22Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope pre-check, if `task.word_count > 45` and `files` contains exactly 1 path, rewrite the task to a compact form capped at 30 words: keep only target file, anchor symbol, and one expected assertion/command. The latest inventory run was 59 words and already triggered timeout reduction; enforce prompt compression before dispatch to cut single-file timeout risk on this repeated smoke-flow pattern.; In `planner.buildPlan`, if the task title starts with `Inventory current decision path` or contains `bounded inventory pattern`, force a 2-step plan only: `inspect_and_write_inventory`, `verify_named_command`. Reject any extra research/implementation step generation. This pattern appeared at least 6 times on the same `apps/cloud-brain/scripts/smoke.mjs` thread and consistently succeeded in 210-244s; keeping it at 2 steps avoids timeout drift from unnecessary planner expansion.; In `planner.buildPlan`, if `files.length == 1`, the task body names an anchor symbol like ``dashboardIncidentFields`` or ``credentialRecoveryRun``, and the requested change is “one focused assertion” or “one deterministic assertion”, force a 2-step `implement`, `verify` plan and ban inventory subtasks unless the immediately previous attempt on the same normalized title failed. This exact assertion-style smoke-flow pattern recurred 8+ times; adding inventory after a timeout increases cycle time without adding new scope information.
- 2026-04-02T21:42:50Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` task-scope precheck, if `task_word_count >= 45` and `files:` lists exactly 1 file, rewrite the execution brief down to `primary_file + one anchor symbol + one required assertion/change + one verify command`, and drop README/schema/spec references from the coder prompt unless they are the target file. This directly matches the repeated `apps/cloud-brain/scripts/smoke.mjs` tasks (59-word inventory task, long verify task) and should cut single-file timeout risk by reducing prompt size before step 1.; In planner fallback logic for self-improve tasks, if the normalized task title prefix repeats within the last `8` project-local tasks and the target file is unchanged, force a `2-step` plan only: `implement_or_inventory` and `verify`; do not emit a separate inspection step. The `verify dashboard incident id` and paired `inventory current decision path` tasks recur 7+ times against the same file, and the extra inspect phase is wasted latency that increases timeout exposure on otherwise bounded work.; In `orchestrator` retry handling, when `failure_kind=timeout` on a task with `files_count=1`, `total_step_attempts <= 3`, and `failed_step` contains `implement the smallest safe change`, auto-generate a reduced retry task capped at `35` words and hard-limit the plan to one edit site plus one verification command. This prevents repeating the broad implementation prompt that timed out on task-167 and should eliminate at least one wasted retry per small-file timeout.
- 2026-04-03T00:00:28Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: Keep prompt changes minimal and tied to repeated evidence.; Prefer prompt rules that improve determinism and verification.; Avoid task-specific prompt tweaks unless the same failure repeats.
- 2026-04-04T20:11:31Z | FAILURE | Add graceful error recovery to queue-worker.sh for orphaned lease cleanup
  Rules: Keep prompt changes minimal and tied to repeated evidence. Prefer rules that improve determinism and verification.; When review_rejection occurs (44% of retries), diff coder output against reviewer objection and inject a targeted fix hint into the retry prompt.; Tasks touching more than 3 files per step should be split — file-count correlates with timeout and step_not_completed failures.
- 2026-04-06T13:15:16Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, if a task title has `>= 18` words or contains `>= 2` separators from `[" -- ", ";", " and ", ", but "]`, rewrite it into an inventory-only or single-edit task before provider dispatch; this directly targets the repeated timeout cluster on `Add shellcheck...`, `Add unit tests...`, and `Extract repeated JSON...` and should cut planner/coder timeout exposure on oversized tasks.; In `orchestrator` before step creation, if the task touches `> 2` files or requests both implementation and validation in one title, cap the generated plan at `2` execution steps plus verification and defer the rest into a follow-up task; enforce this in the planner fallback path to prevent the multi-step coder timeouts seen on `task-012`, `task-014`, and `task-015`.; In `classify_failure` and retry scheduling, treat `total_step_attempts=0` with `duration_seconds <= 65` as `planner_timeout` instead of generic `timeout`, and block unchanged retries after the first occurrence; require the next retry to use a reduced-scope title suffix like `Inventory current decision path...` or `single file only`. This prevents wasted retries from the repeated 60s pre-execution failures.
