- 2026-03-28T07:06:03Z | FAILURE | Inventory current decision path for Feed execution learning back into future provider and task decisions
  Rules: In `agents/planner.sh`, reject any self-improve task whose title or failed step text exceeds 220 characters or contains more than 2 clause separators (`--`, `;`, or 3+ commas); rewrite it into an inventory task with exactly 1 file target and 1 expected artifact before execution. This targets the repeated `review_rejection` pattern on oversized multi-constraint prompts and should cut first-step rejections on inventory/fix tasks.; In `agents/planner.sh`, add a preflight rule for any step containing `Inspect`, `Inventory`, or `identify the narrowest`: the generated plan must be exactly 2 steps max, with step 1 naming one existing file path and one concrete function/branch anchor, and step 2 as verification. If the anchor cannot be named from repo search, classify as `scope_mismatch` and stop. This prevents review rejections caused by vague inspection tasks recurring across 7 inventory tasks.; In `agents/orchestrator.sh`, if a task is marked `review_rejection` on attempt 1 and the step references more than 1 file, automatically rewrite retry attempt 2 to a single-file version using only the first existing path found in workspace; if no existing path is found, classify `missing_source_file` as non-retriable. This converts a dominant rejection class into enforceable scope reduction and avoids wasting the second attempt.
- 2026-03-29T20:12:39Z | SUCCESS | Add canonical incident examples for social, phishing, and authenticity cases
  Rules: In `agents/planner.sh` preflight edit tasks that mention an exact file plus `line`, `Expected:`, or a quoted replacement string: read the target file before planning, and if the expected text is already present classify the task immediately as `no_change_produced`; if the anchor text is absent classify as `missing_source_file`. This would have prevented repeated `review_rejection` retries on the two comment-update tasks that were really anchor/unchanged-state checks.; In `agents/planner.sh` reject any single step whose instruction text exceeds `450` characters or contains `4+` requirement markers (`(a)`, `(b)`, semicolon-separated clauses, or `must:` lists), and auto-split it into inspect/create/verify steps before dispatch. This directly targets the repeated `review_rejection` pattern on bash test creation tasks whose first step bundled full file scaffolding plus assertions into one oversized instruction.; In task registration or planner pre-check, when a task says “model it on”, “following the project pattern”, or references a concrete template/test file, run `test -e` on every referenced path before approval; if any path is missing, block the task as `missing_source_file` instead of letting coder/reviewer discover it later. This addresses the repeated failures around nonexistent test/template anchors and should remove wasted retries on invalid task specs.
