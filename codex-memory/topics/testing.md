  Rules: In `planner/build_steps` and `planner/expand_spec_step`, if a generated step introduces any repo path not present in the task’s `files:` list or a prior existence-checked artifact table, run `test -e` on that path before accepting the step; on the first miss, regenerate once with the missing path removed, and on the second miss, classify immediately as `missing_source_file`. This directly targets the 2 recent `missing_source_file` failures and should prevent wasted coder/reviewer/evaluator cycles on nonexistent files.; In `orchestrator` before step execution, add a `validate_step_paths(step_text)` gate that extracts backticked and plain repo-like paths and rejects the step if more than 1 unchecked path appears in a single-file task. Apply this specifically to verify steps and spec-edit steps. This reduces scope creep from tasks like `scripts/verify-baseline.sh` work that later drifted into `projects/superheld/spec.md` or missing scripts.; In `classify_failure`, add a hard rule: if the failed step contains `No such file`, `not found`, or references a repo path that fails `test -e` before any command starts, force `failure_kind=missing_source_file` and set `retry_allowed=false` for normal retries. This preserves the current 100% retry classification coverage while preventing repeated retries on deterministic path errors.
- 2026-03-31T09:21:15Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In orchestrator step-generation preflight, reject any step whose `paths[]` are not a subset of the task-declared `(files: ...)` list plus files existence-checked in an earlier completed step; when violation count `>=1`, force plan regeneration before coder execution. This directly targets the 2 recent `missing_source_file` failures and should prevent those retries entirely.; In planner fallback for `self-improve` implementation tasks, if the task title contains `Start with` and exactly 1 file path, emit step 1 as `test -e <that file>` plus anchor discovery, and block step 2/3 creation until that check passes. Enforce this in the deterministic planner path used by inventory follow-ups; measurable outcome: converts missing-path failures into zero-cost preflight exits instead of wasting a full attempt.; In verify-step synthesis, only allow commands from a per-task verifier whitelist built from files found by `rg --files` during step 1; if a verify command references a script not in that whitelist, replace it with a file-local verification command or route back to replanning. This addresses both recent `missing_source_file` failures where verification referenced unavailable scripts and should eliminate that failure mode in single-file tasks.
- 2026-03-31T11:06:05Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In task-step generation, if `task.files` contains exactly 1 path, run `test -f <path>` before emitting any implementation or verification step; if the file is missing, mark the task `blocked_missing_source_file` and skip coder dispatch. This directly targets the 2 recent `missing_source_file` failures and should prevent those retries entirely.; In the orchestrator preflight, reject any step whose command references a file path not present in the task’s declared `(files: …)` list unless a prior completed step recorded `exists=true` for that exact path. Enforce this in step validation before step 1 and before each retry; it would have stopped both failures where downstream steps assumed `scripts/verify-baseline.sh` existed.; In failure classification, if a coder/reviewer/evaluator run ends with `total_step_attempts <= 3`, no edited files, and stderr/stdout contains `No such file`, `cannot open`, or `not found`, override the category to `missing_source_file` and set `retriable=false`. This prevents wasted retries on non-executable steps and improves classification precision for the dominant failure mode.
- 2026-03-31T11:12:09Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with apps/cloud-brain/scri
  Rules: In the planner task-to-step compiler, hard-block any task whose `files:` annotation contains exactly 1 path unless `test -e <path>` succeeds before plan generation; emit `missing_source_file` immediately and skip coder dispatch. This directly covers the 2/3 recent failures caused by missing declared targets and should prevent at least 2 wasted full runs per similar batch.; In orchestrator preflight, parse every step for backticked file paths and reject the plan if any path is not a descendant of the task’s declared `files:` list and was not verified by an earlier step-local existence check. Enforce this in the step validator before `Running step 1/...`; this prevents hidden scope creep like verification commands referencing undeclared scripts such as `scripts/verify-baseline.sh`.; In planner step synthesis, if a task title/body contains `Start with <file>` and also names any extra file or command outside that path, force insertion of a step-0 inventory/existence probe that records all required files before any edit or verify step is allowed. Trigger when referenced-path count is `>1`; this converts ambiguous multi-file tasks into validated scope and reduces missing-file retries.
- 2026-03-31T11:18:16Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident payload coverage in smoke flow -- Dire
  Rules: In `orchestrator` preflight, when a task includes `(files: ...)` or a single backticked primary path, run `test -e` on every declared path before step generation; if any path is missing, abort before planner execution and classify `missing_source_file`. This directly targets the 2 recent `missing_source_file` failures and prevents wasting all 3 planned steps on impossible work.; In planner task normalization, if the prompt names exactly 1 primary file but the generated plan references `>=2` additional repo paths or any verify command touching other files, force step 1 to be an inventory/existence-check step that enumerates those dependencies before implementation. Enforce this in the planner fallback path used for self-improve tasks; expected outcome is fewer late-step `missing_source_file` failures from hidden dependencies like `scripts/verify-baseline.sh`.; In `classify_failure`, add a deterministic override: if the failed step text contains `Run \`bash ` or `Run \`node ` and stderr/stdout includes `No such file`, `cannot open`, `MODULE_NOT_FOUND`, or `ENOENT`, classify as `missing_source_file` instead of `missing_dependency` or `unknown`. This should keep retry coverage at 1.00 while reducing wasted retries on file/path problems.
- 2026-03-31T11:28:38Z | SUCCESS | [self-improve:medium] Inventory current decision path for verify trigger-aware credential recovery routing in smoke flow
  Rules: In `orchestrator` preflight, parse every `files:` path from the task text and every literal command path from planned verify steps; if any referenced file does not exist before step 1, abort planning and classify `missing_source_file`. This would have prevented both recent `missing_source_file` failures from reaching verify.; In `planner` task normalization, when a task’s primary edit file and verify command touch different roots (for example `packages/schema/...` edit plus `scripts/verify-baseline.sh` verify), inject a mandatory dependency-check step that runs before implementation and records existence of all verify artifacts. Enforce this in the plan builder for any task whose verify step mentions a path not listed in `files:`.; In `orchestrator` scope gate, if task text exceeds 45 words and contains both `Start with` plus 2 or more backticked file paths, do not only reduce timeout; rewrite the run into an inventory-first task or split into `inventory` then `implement`. The one recent timeout happened at 59 words and already triggered the verbose gate, so the current behavior is too weak.
- 2026-03-31T13:12:20Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` preflight, parse every `(files: ...)` entry and every backticked path ending in `.sh`, `.mjs`, `.json`, `.md`; if any referenced path does not exist at task start, abort before planner dispatch and classify `missing_source_file`. This directly targets the 2 recent `missing_source_file` failures and should prevent 2 wasted full executions per recurrence.; In task generation for self-improve tasks, if the task body contains `Run \`` or `verify` plus a concrete command path not already listed in `(files: ...)`, append that path into the explicit dependency set and run `test -e` on it before step 1. Enforce this in the task-normalizer so verification artifacts like `scripts/verify-baseline.sh` cannot be omitted from dependency checks.; In `classify_failure`, upgrade any failure to `missing_source_file` when the failed step mentions a path-bearing command and the command target is absent after the run, even if the raw result was `timeout` or `review_rejection`. This closes the common misclassification gap where path problems consume retries under the wrong label.
- 2026-03-31T13:22:33Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` task pre-check, parse every backticked path plus every `(files: ...)` entry from the task text; if any referenced file is missing before step generation, abort the task immediately and write `failure_kind=missing_source_file` instead of creating coder/reviewer/evaluator steps. This targets the 2/3 recent failures in the dominant category and should prevent both wasted retries.; In planner step synthesis, extract executable paths from verification commands with regex `(?:bash|node)\s+([^\s]+)`; for each match, run an existence check before Step 1. If the script path is absent, replace the task with a one-step inventory failure and classify `missing_source_file`. Enforce this in the step-builder that produced `bash scripts/verify-baseline.sh`; it would have caught the baseline failure before spending 3 step-attempts.; In retry policy, add a hard block in `classify_failure` or retry admission: if `failure_kind=missing_source_file` and the missing path hash is unchanged from the previous attempt, set `max_retries=0` for that task title. This is enforceable from stored evidence and prevents repeated retries on unchanged absent files.
- 2026-03-31T13:32:49Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with `apps/cloud-brain/scr
  Rules: In the orchestrator pre-check, parse every backticked path plus every `(files: ...)` target from the task text, `stat` each path before step 1, and abort with `missing_source_file` if any required file is absent. This should run before planner invocation and should cut the current 2/30 missing-file failures to near zero while also avoiding wasted planner/coder/reviewer cycles.; Extend the same pre-check to verification commands: when a task mentions `Run ...` or names a script/binary path, extract the executable path and verify it exists and is executable before scheduling the verification step. If the path is missing, classify `missing_source_file` instead of retrying as `timeout` or `review_rejection`; this prevents retries on impossible step-3 validations.; In `classify_failure`, add a hard override: if coder/reviewer/evaluator text contains `ENOENT`, `no such file`, `cannot find module`, or `not found`, and the missing token resolves to a repo-relative path, force classification to `missing_source_file` and attach the missing path in structured metadata. This is enforceable string matching and should improve retry routing for the current 14/20 review-driven retries that may hide environment/path faults.
- 2026-03-31T13:43:53Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, parse every backticked path plus every path inside the task `(files: ...)` suffix, run `fs.existsSync` before step creation, and fail the task immediately with `classification=missing_source_file` if any referenced path is absent. This extends the current explicit-file guard to all task-supplied path channels and should eliminate the repeated 2/2 missing-file retries before execution starts.; In the planner fallback used for self-improve tasks, reject any planned verification command whose primary script/file argument does not exist in the repo root at plan time; write `missing_source_file` into the step record instead of emitting a runnable step. Target the planner function that converts task text into `Run ...` verification steps, and prevent wasted coder/reviewer/evaluator passes on nonexistent smoke or baseline targets.; In retry gating, add a hard block in `classify_failure` or retry admission: if the last failure is `missing_source_file` and no newer commit modified the missing path’s nearest existing parent directory, set `max_retries=0`. This is enforceable from git diff plus the stored missing path, and it prevents repeated retries when the repository state has not changed.
- 2026-03-31T15:27:26Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` task intake, parse every backticked path plus every `(files: ...)` target and run `fs.existsSync` before planner dispatch; if any required path is missing, stop task creation and write `missing_source_file` immediately. This directly targets the dominant 2-task failure mode and should prevent those retries from consuming any planner/coder/reviewer time.; In `build_compact_planner_prompt` and the step generator, forbid verification steps that reference a file or script not present in the validated path set from preflight; if a step mentions ``node <path>`` or ``bash <path>`` and `<path>` was not confirmed to exist, downgrade the task to a 1-step inventory/discovery task. This is enforceable with a regex check on generated commands and should eliminate runnable-step `missing_source_file` failures.; In `classify_failure`, add a hard rule: if stderr/stdout contains `ENOENT`, `No such file`, `cannot find`, or `does not exist` and the missing token matches a path-shaped string, override classification to `missing_source_file` even when reviewer/evaluator disagree. Threshold: 1 path-shaped match is sufficient. This should keep classification coverage at 1.00 and prevent wasted retries under `unknown` or `review_rejection`.
- 2026-03-31T15:36:48Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` preflight, when a task contains a `(files: ...)` clause with 1 or more paths, require `stat()` success for every listed path before provider selection; if any path is missing, abort task creation within 1 step and classify `missing_source_file`. This directly targets the 2/3 recent failures in the dominant category and prevents a full planner/coder/reviewer cycle on impossible work.; In the step-builder that expands prompts like `Start with \`path\` around ...`, extract every backticked token ending in `.mjs`, `.sh`, `.md`, `.json`, `.ts`, or `.js`; if the prompt names more than 2 unique file paths and any extracted path is absent, rewrite the task to a single inventory step instead of an implementation step. This is enforceable with a regex gate and prevents malformed self-improve prompts from becoming failing code tasks.; In `classify_failure`, add a hard rule: if stderr/stdout matches `ENOENT`, `no such file`, `cannot find`, or `does not exist` and the missing token matches any task-declared path, force classification to `missing_source_file` with confidence `1.0`; skip fallback categories. Coverage is already 1.00, so this rule improves precision and avoids misrouting missing-file failures into retryable buckets.
- 2026-03-31T15:47:20Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with `apps/cloud-brain/scr
  Rules: In `orchestrator` preflight, parse every backticked path plus every path inside `(files: ...)`; if any referenced path does not exist on disk before step 1, stop task creation and classify as `missing_source_file` instead of sending it to planner/coder. This directly targets the dominant 2-task failure pattern and should prevent 100% of retries on impossible file targets.; In `planner` fallback generation, when exactly 1 file target is named and it fails the preflight existence check, auto-rewrite the task into a 1-step inventory result only if a sibling path match exists within Levenshtein distance `<= 3` under the same directory; otherwise hard-fail. Enforce this in the planner prompt builder to convert bad-path tasks into bounded discovery instead of wasting a full implementation attempt.; In `classify_failure`, add a deterministic rule: if coder/reviewer/evaluator output contains `ENOENT`, `no such file`, `cannot find`, or `not found` and the missing token matches a referenced task path, classify as `missing_source_file` with retry budget forced to `0`. This improves classification consistency and prevents the current `missing_dependency/unknown` spillover from consuming retries.
- 2026-03-31T15:59:15Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, parse every path from both backticked text and the `(files: ...)` suffix; if the task references `>=2` unique repo paths and any one does not exist at dequeue time, stop before planning, classify as `missing_source_file`, and set `retryable=false`. This hardens the existing missing-file rule against prompt/body drift and should eliminate the current 2/2 missing-source retries.; In the planner fallback used for `"Inventory current decision path"` tasks, if the title contains `Inventory current decision path` or `bounded inventory`, force `max_steps=2` and ban executable verification steps. Enforce this in the deterministic-plan builder, because these inventory tasks have appeared repeatedly and the latest run still spent a third step on an unrelated smoke command; removing that step cuts unnecessary runtime by about 25 to 35 seconds per inventory task and reduces timeout exposure.; In command synthesis for verification steps, require lexical alignment between the generated CLI flag and the task slug: normalize both strings and require at least `2` shared non-stopword tokens, or require one exact referenced file/function token match. If the check fails, do not run the command; classify as `planner_mismatch` instead of `timeout` or generic failure. This directly addresses the latest mismatch where an `incident id` inventory task ran `--verify-credential-recovery-routing`.
- 2026-03-31T17:41:32Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-check, parse every backticked path plus every path inside the task’s `(files: ...)` suffix, deduplicate them, and `stat` all of them before step generation; if any required path is missing, stop within 5s, classify `missing_source_file`, and set `retryable=false` until a later run sees all paths present. This should eliminate the current pattern of wasting full planner/coder/reviewer cycles on impossible file-target tasks.; In `build_compact_planner_prompt`, if the task names exactly 1 writable file in `(files: ...)`, strip all non-target reference files from step 1 and force the first step to inspect only that writable file plus at most 2 read-only reference files named in the prompt; if more than 2 extra references remain, split them into a separate inventory task instead of passing them into the implementation task. This is enforceable in planner prompt assembly and should reduce both timeout pressure and missing-file exposure from over-broad context.; In `classify_failure`, add a deterministic branch: when stderr/stdout contains `ENOENT`, `no such file`, `cannot find`, or `does not exist`, emit `missing_source_file` only if a missing path string is present; otherwise emit `missing_dependency`. This prevents conflating absent repo files with missing runtime tools and should improve retry routing for the 3 recent `missing_dependency` retries plus the existing missing-file failure.
- 2026-03-31T17:52:31Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` pre-check, resolve every path named in `(files: ...)` plus every backticked repository path in the task body with `test -e`; if any are missing, stop before planner dispatch, classify as `missing_source_file`, and set `retryable=false` until the same pre-check passes. This prevents wasting all 3 pipeline stages on non-existent targets.; In `task_builder` for `self-improve` tasks, if the normalized title stem matches a task completed in the last 6 hours and the writable file set is identical, suppress task creation unless at least one referenced source file has a newer mtime than the successful run. This cuts repeated near-duplicate smoke/assertion tasks that currently consume 200 to 450 seconds each.; In `planner` fallback selection, if a task names exactly 1 writable file and at least 1 verification command, force a 2-step plan only: `inspect+edit` and `verify`; skip separate inventory/anchor steps. Enforce in `build_plan` before step emission to reduce timeout exposure on single-file tasks by removing one full coder/reviewer/evaluator cycle.
- 2026-03-31T18:02:31Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with `apps/cloud-brain/scr
  Rules: In `orchestrator` preflight, when a task contains `files:` plus 1 or more backticked paths, resolve every referenced repo path before planner dispatch; if any path is absent, stop the task before step 1, classify `missing_source_file`, and set `retryable=false`. This extends the current single-file guard to multi-reference tasks and should prevent the full planner/coder/reviewer cycle on the repeated file-contract tasks.; In `parse_task_paths` or equivalent prompt parser, add a hard rule: if a task names more than 3 distinct file paths, keep only 1 writable target and at most 2 read-only references; move the rest into `deferred_context`. Enforce this before plan generation for self-improve tasks to cut oversized prompts that already trigger fallback planning and reduced timeouts, targeting the 0.32 timeout failure rate.; In planner fallback logic, when `word_count > 60` or `coordination_markers >= 3`, force a 2-phase plan template: step 1 may inspect only the declared writable file, step 2 may read at most 2 supporting files, final step verifies. Reject any generated step that introduces a new file path not in the preflight set. This is code-enforceable in `planner` and reduces timeout risk from scope creep.
- 2026-03-31T18:13:35Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` preflight, if a task title contains `"(files:"`, extract every listed path and run `fs.existsSync` on each before planning; if any path is missing, stop before step 1, classify as `missing_source_file`, and set `retryable=false`. This turns the current dominant failure mode into a zero-step failure and prevents wasted planner/coder/reviewer time.; In `planner` fallback generation, if the task text matches `Inventory current decision path` or `bounded inventory pattern`, force a 2-step plan only: `inspect/write inventory artifact` and `verify artifact exists`; block any execution step containing `node `, `npm `, `pnpm `, or test/smoke commands for those tasks. The latest successful inventory task still spent a third step on an unrelated smoke run, which is avoidable timeout surface.; In the task-scope gate inside `orchestrator`, when task word count is `> 45` and the task already declares exactly one writable file in `(files: ...)`, cap the plan at `max_steps=3`, `max_refs=3`, and require every generated step to mention that file path or the declared artifact path. This is enforceable with a step-text validator and reduces timeout risk from verbose but actually narrow tasks.
- 2026-03-31T19:55:54Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` pre-check, if a task names exactly 1 writable file and the imperative contains `Add one`, `Add one focused assertion`, or `one deterministic assertion`, cap the execution plan at 2 steps (`edit`, `verify`) and cap timeout at 240s instead of 300s. This pattern appears in 4+ recent `apps/cloud-brain/scripts/smoke.mjs` tasks that all finished in 198-256s except the broader payload-coverage variant that stretched to 375s; removing the inspection step should cut timeout exposure on these narrow tasks by roughly 20-30%.; In planner task-shaping, if the prompt exceeds 60 words or contains 4+ coordination markers and still names only 1 writable file, auto-summarize it to a normalized micro-task before step generation: keep only `{target file, anchor symbol, requested assertion, required verification command}`. Enforce this in the fallback planner path that already detects oversized prompts. This directly targets the repeated smoke-task family where verbose prompts trigger extra planning overhead without adding scope clarity.; In retry orchestration, if the same normalized task stem and same writable file fail with `timeout` once within the last 24h, do not requeue the same implementation task; replace it with an `inventory current decision path` task automatically. The repeated `verify ... in smoke flow` and `inventory current decision path ...` pairs show the bounded-inventory pattern succeeds repeatedly, so this rule should prevent at least one wasted retry for each narrow-file timeout.
- 2026-03-31T20:07:37Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` pre-check, if `task.files` resolves to exactly 1 writable file and the prompt mentions `assert`, `coverage`, `field`, or `schema`, force a 2-step plan only: `edit` then `targeted verify`; skip the separate inventory step unless the same `task_title + file` failed in the last 24h. This removes 1 full step from the repeated smoke/assertion tasks that now dominate superheld and should cut micro-task runtime by about 25 to 35%.; In planner fallback generation, if the normalized task text exceeds 60 words or contains 3 or more coordination markers/backticked refs, rewrite the execution prompt to only 4 fields: `goal`, `target_file`, `edit_anchor`, `verify_command`; drop milestone/spec narration before sending to coder/reviewer/evaluator. The latest successful run still triggered oversized-prompt fallback at 74 words, so enforcing this in the planner reduces timeout pressure on the 32% timeout-failure class.; In task creation for `self-improve` items, if 2 or more recent successful tasks in the last 12h share the same target file and differ only by one schema field or assertion phrase, merge them into one contract-coverage task instead of queuing separate follow-ups. The history shows repeated `apps/cloud-brain/scripts/smoke.mjs` assertion tasks; this rule is enforceable in the generator and reduces scope churn and wasted reviewer cycles.
- 2026-03-31T20:16:52Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with `apps/cloud-brain/scr
  Rules: In `orchestrator` task pre-check, if a task title/body exceeds `60` words or contains `3+` coordination markers (`and`, `but`, `while`, `after`, `before`), force `inventory_first=true` and split execution into `inspect -> edit -> verify`; this matches the repeated smoke-flow tasks that only stabilized once they were reduced to 3 bounded steps and should cut timeout exposure on verbose tasks by keeping edit scope to one anchor file.; In reviewer output validation, reject any `review_rejection` that does not include a structured `cause_code` from `{wrong_file, missing_assertion, missing_verification, scope_expanded, env_blocked}` plus one `target_path`; retry classification is already at `1.00`, so enforcing this in `reviewer`/`classify_failure` should eliminate the remaining high-volume low-signal review loops and prevent wasted retries on the same unchanged strategy.; In `task_builder` or retry planner, if the same normalized task stem appears `>=2` times within `24h` and all instances target the same file (for example repeated `apps/cloud-brain/scripts/smoke.mjs` assertions), auto-convert the next retry into a single-file delta task capped at `1` code change and `1` verification command; this prevents scope creep across repeated micro-tasks and should reduce reviewer rejections caused by over-broad edits.
- 2026-03-31T20:28:09Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task title starts with `Inventory current decision path for` or contains `Do not implement code changes in the same run`, force a 2-step template only: `inspect` and `write inventory artifact`; block any third `verify` step unless the command string contains the exact primary file from `(files: ...)`. This removes off-target verification like the latest run’s `--verify-credential-recovery-routing` step and should cut wasted review/eval cycles on inventory tasks by 1 step per run.; In planner step generation, if task text is longer than 45 words or contains both `Start with` and `Add deterministic assertion`, auto-split into a bounded single-file task plus a separate follow-up verification task, and cap each generated step description at 30 words. The project already shows a 31% timeout rate and repeated verbose smoke-flow tasks; this rule is enforceable before execution and should reduce timeout exposure on long self-improve tasks.; In `reviewer` output validation, reject any review marked `approved` when evaluator score is `0` or `<50` unless the review JSON includes `cause`, `evidence_path`, and `verification_match=true`. Recent history has at least 5 SUCCESS tasks with scores `0/31/32/65/66`, which is a repeated misclassification pattern; forcing structured justification will improve the dominant `review_rejection` retry class and prevent low-signal approvals from being treated as clean successes.
- 2026-03-31T22:00:43Z | SUCCESS | [self-improve:high] Verify dashboard incident id field in smoke flow -- Start with `apps/cloud-brain/scripts/smoke.mjs` 
  Rules: In `orchestrator` step-builder, if the task title/body contains a quoted flag, file anchor, or field name that does not appear in the generated verification command or assertion target, block execution and regenerate step 3 once; enforce with token overlap on the normalized task keywords and verification prompt (`overlap < 2` or missing exact field like `incident_id` => regenerate). This targets the repeated off-target verification pattern that produced `SUCCESS` with score `0`.; In `post-guard` or `task_result_classifier`, reclassify any `SUCCESS` with `score <= 32` as `review_rejection` unless the evaluator text contains both `verified` and the task’s primary target token; this would have caught at least 5 recent low-score “successes” (`task-101` to `task-105`) and improves dominant failure labeling instead of letting bad approvals hide in success metrics.; In `reviewer` approval logic, require a structured approval footer for scoped code-change tasks touching 1 file: `target_file=...`, `target_symbol=...`, `verification_command=...`, `verified_field=...`; if any field is missing or `verified_field` does not match the task noun phrase, downgrade to rejection. This addresses the dominant `review_rejection` class by preventing approvals that are not tied to the requested change.
- 2026-03-31T22:26:46Z | SUCCESS | [self-improve:high] Verify dashboard incident payload coverage in smoke flow -- Start with `apps/cloud-brain/scripts/smo
  Rules: In `orchestrator` task pre-check, if a task title/body repeats the same normalized target string seen in the last 6 hours for the same project and file path, do not enqueue another implementation task; convert it to one inventory task or suppress it entirely. This cuts duplicate review-rejection retries like the repeated `smoke.mjs` verification tasks and prevents at least 1 wasted retry per repeat cluster.; In planner generation, if the normalized task prompt is over 60 words or contains 3 or more coordination markers (`and`, `but`, `while`, `before`, `after`), force a 4-step template: inspect target file, implement one assertion/change, run the exact target verification, summarize evidence. Enforce in the planner fallback path to reduce review rejections from oversized mixed-intent prompts and lower timeout exposure from broad steps.; In verification-step synthesis, extract the primary file/function target from step 2 and require step 3 to include either that file’s named script/test command or a repo search proving the changed symbol is exercised; if not, regenerate the verification step once before execution. This directly prevents the repeated mismatch where verification commands do not clearly cover the edited contract and should reduce review-rejection retries.
- 2026-03-31T22:33:40Z | SUCCESS | [self-improve:medium] Verify trigger-aware credential recovery routing in smoke flow -- Start with `apps/cloud-brain/scr
  Rules: In `plan_self_improve_task`, if the normalized task title plus target file list matches a `SUCCESS` from the last 6 hours and the task edits only 1 file, block a new implementation task and emit an inventory or verification task instead. This directly targets the repeated `apps/cloud-brain/scripts/smoke.mjs` assertion churn and should remove duplicate retries like the repeated `verify-*smoke flow` tasks.; In the step builder inside `orchestrator`, when a task has exactly 1 file in `(files: ...)` and the prompt contains `one deterministic assertion` or `one focused assertion`, enforce a narrow step-2 contract: exactly 1 writable file, exactly 1 new assertion, and no more than 2 read-only reference files. Reject broader plans before execution. This is enforceable scope reduction for the repeated smoke-flow review rejections.; In `retry_with_failure_context` for step 2, when `failure_kind=review_rejection` and the task is single-file, require the retry prompt to include: the rejected file path, 1 concrete anchor near the edit site, and 1 quoted reviewer/evaluator reason; if any of those are missing, do not retry and regenerate the step. Cap the regenerated retry prompt at 600 characters. `task-106` and `task-107` both fit the pattern where attempt 2 succeeded only after enriched retry context.
- 2026-03-31T22:42:36Z | SUCCESS | [self-improve:high] Inventory current decision path for verify dashboard incident id field in smoke flow -- Direct retri
  Rules: In `orchestrator` pre-check, if a task is single-file (`files:` has 1 path) and the title/body references the same target file plus 2 or more secondary contract files, rewrite it into a 2-step bounded task: `inventory` then `implement`, and cap writable scope to the single target file only. This addresses the repeated `apps/cloud-brain/scripts/smoke.mjs` review rejections caused by contract-validation tasks expanding beyond one concrete edit site.; In `build_retry_prompt` and `classify_failure`, when `failure_kind=review_rejection` on a single-file task, require the retry payload to include all 3 fields before requeue: `target_file`, `exact_edit_anchor`, and `rejection_delta` extracted from reviewer text; if any field is missing, generate an inventory task instead of a direct implementation retry. This is enforceable at queue time and should cut wasted review-rejection retries in the dominant category.; In `task_deduper` or strategy generation, if 2 or more open/recent tasks within 12 hours target the same file and share a normalized stem of at least 5 words after removing verbs like `verify`/`inventory`, suppress new implementation tasks and emit at most one follow-up verification task. The history shows the same 3 smoke-flow themes being regenerated repeatedly; this rule reduces scope churn and prevents redundant retries.
