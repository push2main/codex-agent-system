- 2026-03-28T07:06:03Z | FAILURE | Inventory current decision path for Feed execution learning back into future provider and task decisions
  Rules: In `agents/planner.sh`, reject any self-improve task whose title or failed step text exceeds 220 characters or contains more than 2 clause separators (`--`, `;`, or 3+ commas); rewrite it into an inventory task with exactly 1 file target and 1 expected artifact before execution. This targets the repeated `review_rejection` pattern on oversized multi-constraint prompts and should cut first-step rejections on inventory/fix tasks.; In `agents/planner.sh`, add a preflight rule for any step containing `Inspect`, `Inventory`, or `identify the narrowest`: the generated plan must be exactly 2 steps max, with step 1 naming one existing file path and one concrete function/branch anchor, and step 2 as verification. If the anchor cannot be named from repo search, classify as `scope_mismatch` and stop. This prevents review rejections caused by vague inspection tasks recurring across 7 inventory tasks.; In `agents/orchestrator.sh`, if a task is marked `review_rejection` on attempt 1 and the step references more than 1 file, automatically rewrite retry attempt 2 to a single-file version using only the first existing path found in workspace; if no existing path is found, classify `missing_source_file` as non-retriable. This converts a dominant rejection class into enforceable scope reduction and avoids wasting the second attempt.
- 2026-03-30T13:08:41Z | SUCCESS | [self-improve:medium] Define incident-linked learning scope in overview -- Start with docs/overview.md after ## Current 
  Rules: In `orchestrator` task pre-check, if a task title/body contains `Start with` and exactly 1 file path and the generated plan would have more than 2 steps, force a 2-step template: `edit` + `verify`; move any "inspect the surrounding structure" language into the coder prompt instead of a standalone step. This targets the repeated `unknown_persistent` failures on Step 1 inspection tasks (`task-023`, `task-028`) and should cut one full planner/coder/reviewer cycle per doc/schema task.; In `planner` step generation, if a step is read-only and matches `inspect`, `identify insertion point`, `confirm field names`, or `surrounding section structure`, mark it `non_retryable_read_probe`; after 1 failure, auto-convert the parent task into a bounded inventory task instead of retrying the same step. Enforce in `generate_plan` and `retry_decision`. This would have prevented at least 2 wasted retries on `task-023` and `task-028` and improves classification away from `unknown_persistent`.; In `classify_failure`, if `failure_kind` would be `unknown_persistent` and the failed step contains `Run`, `confirm it prints`, `assert`, `rg -n`, `node -e`, or `python - <<`, reclassify using command evidence: `verification_failure` when the command ran but assertions failed, `missing_dependency` when interpreter/binary is absent, `command_construction_failure` when the shell snippet is malformed. Source text must combine coder, reviewer, evaluator, and step command. This directly addresses the dominant unknown bucket seen on `task-023` and `task-025`.
- 2026-03-30T20:45:05Z | SUCCESS | [self-improve:medium] Extend baseline verification for initial learning and slice markers -- Start with scripts/verify-b
  Rules: In `classify_failure` for orchestrator result handling, reclassify any failure with `total_step_attempts <= 1` and `failed_step` containing `read`, `identify`, `insertion point`, or `formatting pattern` from `unknown_persistent` to `planning_failure`. This targets the repeated documentation/inventory false-unknown cases and should cut the dominant `unknown_persistent` bucket by at least 25% in superheld.; In planner pre-check generation, if the task touches exactly 1 file and the prompt already names an anchor like `after ##`, `near`, or `at <symbol>`, forbid a separate inspection-only step and emit a 2-step plan only: `edit` + `verify`. Enforce this in the planner template builder; it would have prevented the failed `docs/architecture/first-slice.md` read-only step and should remove 1 wasted step from similar doc/edit tasks appearing 2+ times.; In `orchestrator` before execution, if a step description is longer than 420 characters or contains more than 2 `and` conjunctions, auto-split it into smaller steps capped at one file mutation plus one assertion block per step. This directly addresses the 0.40 timeout rate and the repeated long-form self-improve tasks that run 230s-345s; target a 10-15% timeout reduction.
