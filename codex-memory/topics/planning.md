- 2026-03-26T12:18:16Z | FAILURE | [self-improve:low] Drain approval backlog -- 12 active approvals are waiting (12 approved). Review pending approvals, pa
  Rules: In `agents/planner.sh`, reject any task title or step prompt over 24 words or containing 3 or more coordination markers (`and`, `with`, commas, parentheses) unless the planner first rewrites it into a maximum 3-step plan with one file target per step; this directly targets the repeated zero-step timeouts on 30-word multi-clause tasks and should cut planner timeout exposure before execution starts.; In `agents/planner.sh` and `agents/orchestrator.sh`, enforce a hard pre-step budget split: planner must emit step 1 within 20s, full plan within 45s, and abort at 60s with a deterministic JSON handoff task containing only the first bounded step; 23 recent timeouts were planner-dominated, so this rule prevents another full-worker-budget loss when planning stalls.; In `agents/planner.sh`, add a repository-grounding precheck for inspection tasks: if the prompt names variables/functions/files, the planner must run a fast symbol existence scan and rewrite missing identifiers out of the step before dispatch; if 2 or more named identifiers are absent, classify as `scope_mismatch` instead of sending to coder. This would have prevented the `pending_approval_tasks`/`approved_tasks` miss that burned 2 attempts on `scripts/multi-queue.sh`.
- 2026-03-29T20:10:08Z | SUCCESS | In agents/planner.sh, add a comment at line 1014 documenting the MAX_STEP_CHARS=600 gate
  Rules: In `agents/planner.sh`, reject or auto-rewrite any step whose text contains `Expected:` plus a claim that the target state already exists (`already`, `already exists`, `already reads`, `confirm`, `inspect only`) when the queue title asks for a code change; classify this as `plan_noop_mismatch` before coder dispatch. This targets the repeated review rejections on `task-004`, `task-006`, and `task-008` where Step 1 only rediscovered that no edit was needed.; In `agents/planner.sh`, add a preflight gate for edit tasks: if the title matches `add`, `update`, `change`, `document`, `fix`, or `create`, Step 1 must contain exactly one file target and one concrete write verb, and must not exceed 220 chars; otherwise split or fail planning. This is enforceable in the step-scope validator and should reduce review-rejection churn from over-specified Step 1 prompts seen in `task-002`, `task-003`, and `task-007`.; In `scripts/lib.sh` `classify_retry_failure`, add a deterministic `anchor_mismatch_noop` bucket triggered by reviewer/evaluator text containing `already satisfied`, `already exists`, `no changes needed`, `anchor missing`, or `requested state already exists`; mark it non-retriable at write time. This would have prevented wasted second attempts on at least the repeated planner-comment and learner-comment tasks.
- 2026-03-29T20:16:08Z | SUCCESS | Add test: verify planner output steps are each under 600 characters
  Rules: In `scripts/lib.sh` task creation or approval pre-check, reject any step whose instruction text contains `already exists`, `confirm`, `inspect`, or `read` and does not also contain an edit verb (`change`, `add`, `remove`, `create`, `update`); classify it as `no_change_produced` before dispatch. This targets the repeated review rejections on comment/anchor confirmation tasks and should prevent at least the 4 no-op retries seen on `task-004`, `task-006`, and `task-008`.; In `agents/planner.sh`, add a step linter that fails planning when a single step includes both file inspection anchors and exact replacement text, detected by `(line|lines|around line|inspect|read)` plus `(replace|change|edit|create)` in the same step; force a split into 2 steps with verification last. This directly addresses the repeated `review_rejection` pattern from overloaded step 1 prompts and should reduce reviewer ambiguity across the 12 recent rejections.; In `scripts/lib.sh` before dispatch, validate every referenced exemplar path from phrases like `model it on`, `modeled on`, `same boilerplate`, or `same structure`; if the named file does not exist, fail immediately as `missing_source_file` with the missing path. This would have prevented the repeated failures on `tests/planner-fallback-learning.sh` and similar template-dependent tasks instead of wasting 2 attempts.
- 2026-03-29T23:51:58Z | SUCCESS | [self-improve:critical] Inventory current decision path for cap pre-step planning budget -- Start with `packages/schema/
  Rules: In task-generation code for self-improve prompts, add a file-extension consistency guard: if `files:` names `*.json` but any generated step body references the same basename with a different extension (for example `incident.schema.js` vs `incident.schema.json`), reject the plan before execution and regenerate the step text from the canonical `files:` entry. Enforce in the planner/orchestrator pre-check that every referenced path must exactly match one declared file path; this directly targets the repeated `missing_source_file` failures on `packages/schema/incident.schema.json` and should eliminate that entire class of basename-drift retries.; In orchestrator step validation, require a filesystem existence check for every backticked or linked path before coder dispatch; if any path is absent, stop at step 0 with `missing_source_file` and mark the step non-retriable after 1 identical miss. Store a normalized fingerprint of the missing path set in retry classification so the same bad step text cannot run twice. This is enforceable in the pre-execution validator and would have prevented at least the 2 repeated `incident.schema.js` misses plus other repeated missing-file retries.; In planner prompt construction, add a hard cap for self-improve task bodies: if task title + rationale + injected evidence exceed 900 characters, rewrite the task into an inventory-only step with exactly 1 file and 1 expected artifact path before planning. Enforce in the self-improve task synthesizer and planner input builder. This targets the 60s zero-step planner timeouts and the 1-2s planning failures by reducing prompt size before the planner runs.
- 2026-03-30T00:00:09Z | SUCCESS | [self-improve:high] Break retry churn -- Start with packages/schema/incident.schema.json. 1 tasks consumed 1 extra step 
  Rules: In the planner pre-check that builds self-improve tasks, if `declared files` contains any `*.json` path, reject any generated step text that references the same stem with a different extension (for example `incident.schema.js` vs `incident.schema.json`) and regenerate once with the exact declared path only; this directly targets the repeated `missing_source_file` failures on `packages/schema/incident.schema.json` and should prevent at least 2 recent wasted retries.; In `orchestrator` plan validation, add a file-existence gate before step execution: for each non-verification step, extract all path-like tokens from the step text, require `unique_paths <= 1`, and require `fs.existsSync()` for that path in the project root; if the path is missing, fail the task immediately as `missing_source_file` and mark retryable=`false` after the first identical `(task_title,path)` pair. This should cut repeated missing-file churn across the 6 dominant failures.; In the self-improve task generator, if a task title/body contains both `Start with <file>` and inventory-style wording (`inventory`, `decision path`, `inspect`, `identify`, `exact files/functions/decision points`) while the target file extension is `.json`, force the plan template to use file-structure language only (`root object`, `array`, `property`, `example entry`) and forbid terms like `function`, `branch`, or `state transition`; this would have blocked the bad `.js`-oriented inventory steps that failed twice on the same schema task.
- 2026-03-30T00:21:17Z | SUCCESS | Step 1: In packages/schema/incident.schema.json, inspect the root examples array and the existing incident example objec
  Rules: In planner pre-check for `project=superheld`, if a task declares exactly one file with extension `.json`/`.yaml`/`.yml` and any generated step references a sibling code path (`.js`, `.ts`, `.sh`) or code-only anchors (`function`, `branch`, `state transition`), reject the plan and regenerate once with the declared file list only; this directly targets the repeated `incident.schema.json` -> `incident.schema.js` misrouting seen in 2+ `missing_source_file` failures.; In `orchestrator` before coder execution, run `require_file` on every backticked path extracted from each non-verify step; if any path is missing, rewrite the failure as `missing_source_file` with the missing path attached and stop retries after the first attempt for that path/task pair. This should prevent at least 2 wasted retries per repeated bad-path task and improve dominant-category precision.; In task generation for self-improve tasks, if the title/body contains both discovery verbs (`inspect`, `identify`, `confirm`, `inventory`) and implementation verbs (`add`, `append`, `insert`, `apply`, `implement`), split into two tasks before queueing unless the file list is one file and the total step count is already 3 with a separate verify step. This addresses the repeated scope-mixing pattern behind the `missing_source_file` and `unknown_persistent` failures.
