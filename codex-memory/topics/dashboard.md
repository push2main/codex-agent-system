- 2026-03-24T20:25:07Z | FAILURE | Inventory current state for Generate bounded successor UI tasks from failed dashboard epics until the requirement
  Rules: Prefer rules that target repeated cross-task failures, not one dashboard epic or one field.; When a task is inventory-only, state explicitly: inspect named files, write one compact artifact, and do not implement code changes.; Require each step to name exact files, outputs, and verification commands so planner/coder/reviewer stay aligned.
- 2026-03-29T20:06:43Z | SUCCESS | Turn the web README into a concrete family dashboard blueprint
  Rules: In `agents/planner.sh`, reject any step whose instruction text exceeds `450` characters or contains `8+` comma-separated sub-requirements; auto-rewrite it into at most `2` sentences plus a separate verify step. This targets the repeated `review_rejection` pattern from overloaded step-1 prompts and should cut first-pass reviewer retries on test/comment tasks.; In `agents/planner.sh` task synthesis, if the title starts with `Add test:` or `Add unit test:`, require the step to reference exactly `1` existing example test file found via `rg --files tests` and cap implementation scope to `1` new test file plus `1` verification command. If no example file exists, fail planning as `missing_source_file` before execution instead of generating a speculative test recipe.; In `agents/reviewer.sh`, classify step output as `review_rejection` with reason `anchor_already_satisfied` when the requested comment/text already exists or the requested change is already true in the inspected lines; then mark the task non-retriable after the first attempt. This addresses the repeated failures where the task asked to edit comments that already matched the repo state and prevents wasted retry churn.
