- 2026-03-29T20:20:30Z | FAILURE | [self-improve:high] Reduce timeout rate -- Tasks are timing out at 47%
  Rules: In `agents/planner.sh`, reject any proposed step whose text exceeds `400` characters or contains more than `2` semicolons or more than `1` parenthetical list marker (`(a)`, `(b)`, etc.); auto-split it before dispatch and log `step_overscoped`. This directly targets the repeated `review_rejection` tasks caused by overloaded Step 1 instructions and should cut planner/reviewer ambiguity across the 6+ long-step failures.; In `agents/planner.sh` preflight, if a task title starts with `[self-improve:` and has more than `12` words or contains both `--` and `%`, force a two-step cap (`diagnose`, `edit+verify`) and raise planner timeout from `60s` to `120s`; otherwise block queueing with `scope_too_broad_for_self_improve`. This addresses the zero-step planner timeout seen twice on the same timeout-reduction task and reduces timeout waste on verbose self-improve work.; In `scripts/lib.sh` `classify_retry_failure`, map reviewer text containing `already exists`, `already matches`, `no change needed`, `comment already says`, or `confirm the comment block already exists` to `no_change_produced` instead of `review_rejection`. This is enforceable via substring matching and would have reclassified at least 5 repeated review failures that were really no-op/comment-anchor tasks, preventing wasted retries.
