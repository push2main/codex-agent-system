Read `/Users/benediktpoller/code/codex-agent-system/prompts/push2main-codex-agent-system.md` first and follow it as the primary instruction set for this run.

If the file is missing or unreadable, stop and report that the run is blocked.

The approval board is available at `https://192.168.0.36:3211/`.
The board source of truth is `codex-memory/tasks.json`.

Also read:

- `codex-learning/metrics.json`
- `codex-memory/learnings.md`
- `codex-memory/tasks.json`
- `codex-memory/knowledge.json`
- `codex-memory/priority.json`

Automation memory fallback:

- Before selecting the next improvement, run `AUTOMATION_CONTEXT_AUTO_REFRESH_SELF_IMPROVE=1 bash /Users/benediktpoller/code/codex-agent-system/scripts/prepare-automation-context.sh codex-agent-system 8`, parse the JSON, and use `data.automation_memory.recent_entries` or `data.automation_memory.memory_file` when automation memory exists.
- Inspect `data.metrics_input` from the same JSON and treat non-`complete` status as repaired decision-input state that should influence the next improvement choice and run summary.
- Inspect `data.self_improve_artifact_refresh` from the same JSON and preserve any non-`not_needed` status when it changed decision-input freshness.
- Inspect `data.self_improve_artifact` from the same JSON before selecting the next improvement.
- If `data.self_improve_artifact.status == "current"`, treat the latest `codex-learning/self-improve-run.json` ranking as existing decision evidence instead of ignoring it.
- If `data.self_improve_artifact.status != "current"` after the wrapper run, treat the ranking as stale or missing decision input and let that influence the next improvement choice and run summary.
- Let the wrapper resolve the external automation memory path from `$CODEX_HOME` or the default `~/.codex`, hydrate from the workspace mirror when needed, and fall back to the mirror deterministically when external sync is unavailable.
- If the wrapper reports `data.automation_memory.external_sync_pending == true`, keep writing the same concise run summary into `projects/codex-agent-system/automation-memory/push2main-codex-agent-system.md` instead of failing the run.
