# Prompt Rules

- Keep prompt changes minimal and tied to repeated evidence.
- Prefer prompts that ask for deterministic outputs and explicit checks.
- Avoid task-specific prompt tuning unless the same failure recurs.
